---
title: Button
description:
  A clickable element that triggers an action. Supports loading, toggle, and
  icon slots.
sidebar_label: Button
sidebar_position: 2
slug: /components/button
---

import LiveCode from '@site/src/components/LiveCode';
import { Button, ReactSparDemoRoot } from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const buttonScope = { Button };

{/* prettier-ignore-start */}

export const buttonDemoCss = String.raw`
.button-demo {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
}
`;

export const playgroundDemo = String.raw`
function PlaygroundDemo() {
  return (
    <div className="button-demo">
      <Button>Primary</Button>
      <Button variant="secondary">Secondary</Button>
      <Button appearance="outlined">Outlined</Button>
      <Button appearance="text">Text</Button>
      <Button variant="danger">Danger</Button>
    </div>
  );
}

render(<PlaygroundDemo />);
`;

export const sizesDemo = String.raw`
function SizesDemo() {
  return (
    <div className="button-demo">
      <Button size="small">Small</Button>
      <Button size="base">Base</Button>
      <Button size="large">Large</Button>
    </div>
  );
}

render(<SizesDemo />);
`;

export const loadingDemo = String.raw`
function LoadingDemo() {
  const [isLoading, setIsLoading] = React.useState(false);

  return (
    <div className="button-demo">
      <Button isLoading>Saving…</Button>
      <Button variant="secondary" isLoading>Loading</Button>
      <Button
        variant="primary"
        isLoading={isLoading}
        onClick={() => {
          setIsLoading(true);
          setTimeout(() => setIsLoading(false), 2000);
        }}
      >
        Click to load
      </Button>
    </div>
  );
}

render(<LoadingDemo />);
`;

export const disabledDemo = String.raw`
function DisabledDemo() {
  return (
    <div className="button-demo">
      <Button disabled>Disabled</Button>
      <Button variant="secondary" disabled>Disabled</Button>
    </div>
  );
}

render(<DisabledDemo />);
`;

export const toggleDemo = String.raw`
function ToggleButtonDemo() {
  const [isPressed, setIsPressed] = useState(false);

  return (
    <div className="button-demo">
      <Button
        variant={isPressed ? 'primary' : 'secondary'}
        isPressed={isPressed}
        onPressedChange={setIsPressed}
      >
        {isPressed ? 'On' : 'Off'}
      </Button>
    </div>
  );
}

render(<ToggleButtonDemo />);
`;

export const iconsDemo = String.raw`
function IconsDemo() {
  const StarIcon = () => (
    <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
      <path d="M8 1.5l1.85 3.75 4.15.6-3 2.93.71 4.12L8 10.77 4.29 12.9l.71-4.12-3-2.93 4.15-.6L8 1.5z" />
    </svg>
  );

  const ArrowIcon = () => (
    <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
      <path d="M3 8h10M9 4l4 4-4 4" />
    </svg>
  );

  return (
    <div className="button-demo">
      <Button startContent={<StarIcon />}>Favorite</Button>
      <Button endContent={<ArrowIcon />}>Continue</Button>
      <Button startContent={<StarIcon />} endContent={<ArrowIcon />}>Both</Button>
      <Button startContent={<StarIcon />} rounded />
    </div>
  );
}

render(<IconsDemo />);
`;

{/* prettier-ignore-end */}

# Button

`Button` triggers an action when clicked. It wraps the Spar headless button
primitive and adds Takeoff visual vocabulary — variant, size, loading state, and
icon slots.

## Usage

```tsx
import { Button } from '@takeoff-ui/react-spar';
```

```tsx
<Button variant="primary" size="base">
  Click me
</Button>
```

## Playground

<LiveCode
  code={playgroundDemo}
  cssCode={buttonDemoCss}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Sizes

<LiveCode
  code={sizesDemo}
  cssCode={buttonDemoCss}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Loading

<LiveCode
  code={loadingDemo}
  cssCode={buttonDemoCss}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Disabled

<LiveCode
  code={disabledDemo}
  cssCode={buttonDemoCss}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Toggle

<LiveCode
  code={toggleDemo}
  cssCode={buttonDemoCss}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Icons

<LiveCode
  code={iconsDemo}
  cssCode={buttonDemoCss}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Accessibility & Keyboard

- Renders a native `<button>` element by default with correct `type="button"`.
- `loading` surfaces `aria-busy="true"` and `aria-live="polite"` for screen
  readers.
- Toggle mode exposes `aria-pressed` reflecting the current state.
- Disabled buttons set `tabIndex={-1}` and the native `disabled` attribute.

| Key                                 | Behavior                   |
| ----------------------------------- | -------------------------- |
| <kbd>Enter</kbd> / <kbd>Space</kbd> | Activate the button.       |
| <kbd>Tab</kbd>                      | Move focus to/from button. |

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Button {#button}

See
[Spar Button docs](https://spar.app.turkishtechlab.com/docs/Components/Button)
for primitive behavior.

#### Props {#button-props}

| Name                              | Type                                                                                                                        | Default   | Description                                                                                                                         |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />     | <code>React.ReactNode</code>                                                                                                | -         | Button label content.                                                                                                               |
| <ApiBadge label="variant" />      | <code><a href="#button-td-buttonvariant">ButtonVariant</a></code>                                                           | 'primary' | Color variant.                                                                                                                      |
| <ApiBadge label="appearance" />   | <code><a href="#button-td-buttonappearance">ButtonAppearance</a></code>                                                     | 'filled'  | Visual appearance.                                                                                                                  |
| <ApiBadge label="size" />         | <code><a href="#button-td-buttonsize">ButtonSize</a></code>                                                                 | 'base'    | Size scale.                                                                                                                         |
| <ApiBadge label="rounded" />      | <code>boolean</code>                                                                                                        | false     | Renders a pill-shaped (circular) button. Ideal for icon-only actions.                                                               |
| <ApiBadge label="startContent" /> | <code>React.ReactNode</code>                                                                                                | -         | Content rendered before children — typically an icon, but accepts any node (spinner, badge, kbd, etc.). Wrapped in the `icon` slot. |
| <ApiBadge label="endContent" />   | <code>React.ReactNode</code>                                                                                                | -         | Content rendered after children. Same shape as `startContent`.                                                                      |
| <ApiBadge label="classNames" />   | <code>Partial&lt;Record&lt;<a href="#button-td-buttonslot">ButtonSlot</a>, string&gt;&gt;</code>                            | -         | Per-slot extra classes.                                                                                                             |
| <ApiBadge label="slotProps" />    | <code>Partial&lt;Record&lt;<a href="#button-td-buttonslot">ButtonSlot</a>, HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -         | Per-slot HTML-attribute overrides.                                                                                                  |
| <ApiBadge label="isLoading" />    | <code>boolean</code>                                                                                                        | false     | Loading state with screen reader support                                                                                            |
| <ApiBadge label="isPressed" />    | <code>boolean</code>                                                                                                        | undefined | Toggle state - when defined, creates a toggle button                                                                                |
| <ApiBadge label="className" />    | <code>string</code>                                                                                                         | -         | Appends custom classes to the root slot of this part.                                                                               |

#### Events {#button-events}

| Name                                 | Type                                       | Default | Description                              |
| ------------------------------------ | ------------------------------------------ | ------- | ---------------------------------------- |
| <ApiBadge label="onPressedChange" /> | <code>(pressed: boolean) =&gt; void</code> | -       | Callback fired when toggle state changes |

#### Data attributes {#button-data-attributes}

| Attribute                                       | Applied when            | Purpose                                                           |
| ----------------------------------------------- | ----------------------- | ----------------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always                  | Stable selector for wrapper styling on the root slot.             |
| <ApiBadge label="data-variant" />               | Always                  | Reflects the resolved `variant` prop for theme recipe scoping.    |
| <ApiBadge label="data-type" />                  | Always                  | Reflects the resolved `appearance` prop for theme recipe scoping. |
| <ApiBadge label="data-size" />                  | Always                  | Reflects the resolved `size` prop for theme recipe scoping.       |
| <ApiBadge label="data-rounded" />               | When `rounded` is true  | Styling hook for the pill-shaped state.                           |
| <ApiBadge label="data-icon-only" />             | When icon-only          | Auto-detected when icons exist but no children.                   |
| <ApiBadge label="data-loading" />               | When `loading` is true  | Styling hook for the loading state.                               |
| <ApiBadge label="data-disabled" />              | When `disabled` is true | Styling hook for the disabled state.                              |

### Type Definitions {#button-type-definitions}

| Name                                                                               | Definition                                                                                                                                               |
| ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <span id="button-td-buttonvariant"><ApiBadge label="ButtonVariant" /></span>       | <code>'primary' &#124; 'secondary' &#124; 'neutral' &#124; 'info' &#124; 'success' &#124; 'danger' &#124; 'warning' &#124; 'white' &#124; 'black'</code> |
| <span id="button-td-buttonappearance"><ApiBadge label="ButtonAppearance" /></span> | <code>'filled' &#124; 'filledLight' &#124; 'outlined' &#124; 'text'</code>                                                                               |
| <span id="button-td-buttonsize"><ApiBadge label="ButtonSize" /></span>             | <code>'small' &#124; 'base' &#124; 'large'</code>                                                                                                        |
| <span id="button-td-buttonslot"><ApiBadge label="ButtonSlot" /></span>             | <code>'root' &#124; 'content' &#124; 'label' &#124; 'spinner'</code>                                                                                     |

{/* api-tables:end */}
