---
title: Button
description:
  A clickable element that triggers an action. Supports loading, toggle, and
  icon slots.
sidebar_label: Button
sidebar_position: 2
slug: /components/button
---

import LiveCode from '@site/src/components/LiveCode';
import { Button, ReactSparDemoRoot } from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const buttonScope = { Button };

{/* prettier-ignore-start */}

export const buttonDemoCss = String.raw`
.button-demo {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
}
`;

export const playgroundDemo = String.raw`
<div className="button-demo">
  <Button>Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button appearance="outlined">Outlined</Button>
  <Button appearance="text">Text</Button>
  <Button variant="danger">Danger</Button>
</div>
`;

export const sizesDemo = String.raw`
<div className="button-demo">
  <Button size="small">Small</Button>
  <Button size="base">Base</Button>
  <Button size="large">Large</Button>
</div>
`;

export const loadingDemo = String.raw`
function LoadingDemo() {
  const [loading, setLoading] = React.useState(false);

  return (
    <div className="button-demo">
      <Button loading>Saving…</Button>
      <Button variant="secondary" loading>Loading</Button>
      <Button
        variant="primary"
        loading={loading}
        onClick={() => {
          setLoading(true);
          setTimeout(() => setLoading(false), 2000);
        }}
      >
        Click to load
      </Button>
    </div>
  );
}

render(<LoadingDemo />);
`;

export const disabledDemo = String.raw`
<div className="button-demo">
  <Button disabled>Disabled</Button>
  <Button variant="secondary" disabled>Disabled</Button>
</div>
`;

export const toggleDemo = String.raw`
function ToggleButtonDemo() {
  const [isPressed, setIsPressed] = useState(false);

  return (
    <div className="button-demo">
      <Button
        variant={isPressed ? 'primary' : 'secondary'}
        isPressed={isPressed}
        onPressedChange={setIsPressed}
      >
        {isPressed ? 'On' : 'Off'}
      </Button>
    </div>
  );
}

render(<ToggleButtonDemo />);
`;

export const iconsDemo = String.raw`
const StarIcon = () => (
  <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
    <path d="M8 1.5l1.85 3.75 4.15.6-3 2.93.71 4.12L8 10.77 4.29 12.9l.71-4.12-3-2.93 4.15-.6L8 1.5z" />
  </svg>
);

const ArrowIcon = () => (
  <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
    <path d="M3 8h10M9 4l4 4-4 4" />
  </svg>
);

render(
  <div className="button-demo">
    <Button startIcon={<StarIcon />}>Favorite</Button>
    <Button endIcon={<ArrowIcon />}>Continue</Button>
    <Button startIcon={<StarIcon />} endIcon={<ArrowIcon />}>Both</Button>
    <Button startIcon={<StarIcon />} rounded />
  </div>
);
`;

{/* prettier-ignore-end */}

# Button

`Button` triggers an action when clicked. It wraps the Spar headless button
primitive and adds Takeoff visual vocabulary — variant, size, loading state, and
icon slots.

## Usage

```tsx
import { Button } from '@takeoff-ui/react-spar';
```

```tsx
<Button variant="primary" size="base">
  Click me
</Button>
```

## Playground

<LiveCode
  code={playgroundDemo}
  cssCode={buttonDemoCss}
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Sizes

<LiveCode
  code={sizesDemo}
  cssCode={buttonDemoCss}
  editable={false}
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Loading

<LiveCode
  code={loadingDemo}
  cssCode={buttonDemoCss}
  editable={false}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Disabled

<LiveCode
  code={disabledDemo}
  cssCode={buttonDemoCss}
  editable={false}
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Toggle

<LiveCode
  code={toggleDemo}
  cssCode={buttonDemoCss}
  editable={false}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Icons

<LiveCode
  code={iconsDemo}
  cssCode={buttonDemoCss}
  editable={false}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={buttonScope}
/>

## Accessibility & Keyboard

- Renders a native `<button>` element by default with correct `type="button"`.
- `loading` surfaces `aria-busy="true"` and `aria-live="polite"` for screen
  readers.
- Toggle mode exposes `aria-pressed` reflecting the current state.
- Disabled buttons set `tabIndex={-1}` and the native `disabled` attribute.

| Key                                 | Behavior                   |
| ----------------------------------- | -------------------------- |
| <kbd>Enter</kbd> / <kbd>Space</kbd> | Activate the button.       |
| <kbd>Tab</kbd>                      | Move focus to/from button. |

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Button {#button}

See
[Spar Button docs](https://spar.app.turkishtechlab.com/docs/Components/Button)
for primitive behavior.

#### Props {#button-props}

| Name                            | Type                                                                                    | Default   | Description                                                           |
| ------------------------------- | --------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------- |
| <ApiBadge label="children" />   | <code>React.ReactNode</code>                                                            | -         | Button label content.                                                 |
| <ApiBadge label="variant" />    | <code>ButtonVariant</code>                                                              | 'primary' | Color variant.                                                        |
| <ApiBadge label="appearance" /> | <code>ButtonAppearance</code>                                                           | 'filled'  | Visual appearance.                                                    |
| <ApiBadge label="size" />       | <code>ButtonSize</code>                                                                 | 'base'    | Size scale.                                                           |
| <ApiBadge label="rounded" />    | <code>boolean</code>                                                                    | false     | Renders a pill-shaped (circular) button. Ideal for icon-only actions. |
| <ApiBadge label="loading" />    | <code>boolean</code>                                                                    | false     | Loading state with screen reader support.                             |
| <ApiBadge label="startIcon" />  | <code>React.ReactNode</code>                                                            | -         | Icon placed before children.                                          |
| <ApiBadge label="endIcon" />    | <code>React.ReactNode</code>                                                            | -         | Icon placed after children.                                           |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;ButtonSlot, string&gt;&gt;</code>                            | -         | Per-slot extra classes.                                               |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;ButtonSlot, HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -         | Per-slot HTML-attribute overrides.                                    |
| <ApiBadge label="isPressed" />  | <code>boolean</code>                                                                    | undefined | Toggle state - when defined, creates a toggle button                  |
| <ApiBadge label="className" />  | <code>string</code>                                                                     | -         | Appends custom classes to the root slot of this part.                 |

#### Events {#button-events}

| Name                                 | Type                                       | Default | Description                              |
| ------------------------------------ | ------------------------------------------ | ------- | ---------------------------------------- |
| <ApiBadge label="onPressedChange" /> | <code>(pressed: boolean) =&gt; void</code> | -       | Callback fired when toggle state changes |

#### Data attributes {#button-data-attributes}

| Attribute          | Applied when            | Purpose                                                           |
| ------------------ | ----------------------- | ----------------------------------------------------------------- |
| `data-slot="root"` | Always                  | Stable selector for wrapper styling on the root slot.             |
| `data-variant`     | Always                  | Reflects the resolved `variant` prop for theme recipe scoping.    |
| `data-type`        | Always                  | Reflects the resolved `appearance` prop for theme recipe scoping. |
| `data-size`        | Always                  | Reflects the resolved `size` prop for theme recipe scoping.       |
| `data-rounded`     | When `rounded` is true  | Styling hook for the pill-shaped state.                           |
| `data-icon-only`   | When icon-only          | Auto-detected when icons exist but no children.                   |
| `data-loading`     | When `loading` is true  | Styling hook for the loading state.                               |
| `data-disabled`    | When `disabled` is true | Styling hook for the disabled state.                              |

{/* api-tables:end */}
