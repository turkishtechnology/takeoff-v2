---
title: Switch
description:
  Lets users turn a single setting on or off, especially for preferences,
  permissions, and form options.
sidebar_label: Switch
sidebar_position: 2
slug: /components/switch
---

import LiveCode from '@site/src/components/LiveCode';
import {
  Field,
  ReactSparDemoRoot,
  Switch,
} from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const switchScope = { Field, Switch };

{/* prettier-ignore-start */}

export const switchDemoCss = String.raw`
.switch-demo-stack {
  display: grid;
  width: min(100%, 520px);
  gap: 14px;
}

/* Lays out Field.Label and Switch side-by-side, with Field.Description /
   Field.ErrorMessage wrapping to a second row underneath the label. */
.switch-demo-field-row {
  display: grid;
  grid-template-columns: 1fr auto;
  column-gap: 16px;
  row-gap: 4px;
  align-items: center;
}

.switch-demo-field-row > :not(:first-child):not(:nth-child(2)) {
  grid-column: 1 / -1;
}

.switch-demo-inline {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 18px;
}

.switch-demo-status {
  width: fit-content;
  border: 1px solid var(--border-light);
  border-radius: 999px;
  background: var(--static-light);
  color: var(--text-dark);
  font-size: 13px;
  font-weight: 600;
  line-height: 1;
  padding: 8px 12px;
}

.switch-demo-form {
  display: grid;
  width: min(100%, 520px);
  gap: 14px;
}

.switch-demo-actions {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 12px;
}

.switch-demo-button {
  border: 0;
  border-radius: 8px;
  background: var(--primary-base);
  color: var(--static-white);
  cursor: pointer;
  font: inherit;
  font-weight: 600;
  padding: 10px 16px;
}
`;

export const playgroundDemo = String.raw`
<Field className="switch-demo-field-row">
  <Field.Label>Flight status alerts</Field.Label>
  <Switch defaultChecked>
    <Switch.Indicator />
  </Switch>
  <Field.Description>
    We will let you know about delays, gate changes, and cancellations.
  </Field.Description>
</Field>
`;

export const fieldWithSwitchDemo = String.raw`
<div className="switch-demo-stack">
  <Field required className="switch-demo-field-row">
    <Field.Label>Email alerts</Field.Label>
    <Switch defaultChecked>
      <Switch.Indicator />
    </Switch>
  </Field>

  <Field className="switch-demo-field-row">
    <Field.Label>SMS alerts</Field.Label>
    <Switch>
      <Switch.Indicator />
    </Switch>
    <Field.Description>
      Standard messaging rates may apply.
    </Field.Description>
  </Field>

  <Field invalid className="switch-demo-field-row">
    <Field.Label>Push notifications</Field.Label>
    <Switch>
      <Switch.Indicator />
    </Switch>
    <Field.ErrorMessage>
      Enable browser notifications to turn this on.
    </Field.ErrorMessage>
  </Field>

  <Field disabled className="switch-demo-field-row">
    <Field.Label>Disabled setting</Field.Label>
    <Switch>
      <Switch.Indicator />
    </Switch>
  </Field>

  <Field readOnly className="switch-demo-field-row">
    <Field.Label>Read-only setting</Field.Label>
    <Switch defaultChecked>
      <Switch.Indicator />
    </Switch>
  </Field>
</div>
`;

export const controlledDemo = String.raw`
function ControlledSwitchDemo() {
  const [enabled, setEnabled] = useState(true);

  return (
    <div className="switch-demo-stack">
      <div className="switch-demo-status">
        Notifications {enabled ? 'enabled' : 'paused'}
      </div>
      <Field className="switch-demo-field-row">
        <Field.Label>Flight status notifications</Field.Label>
        <Switch checked={enabled} onChange={setEnabled}>
          <Switch.Indicator />
        </Switch>
      </Field>
    </div>
  );
}

render(<ControlledSwitchDemo />);
`;

export const sizeDemo = String.raw`
<div className="switch-demo-inline">
  <Switch aria-label="Extra small switch" size="xsmall">
    <Switch.Indicator />
  </Switch>
  <Switch aria-label="Small switch" size="small">
    <Switch.Indicator />
  </Switch>
  <Switch aria-label="Base switch" size="base" defaultChecked>
    <Switch.Indicator />
  </Switch>
  <Switch aria-label="Large switch" size="large" defaultChecked>
    <Switch.Indicator />
  </Switch>
  <Switch aria-label="Extra large switch" size="xlarge" defaultChecked>
    <Switch.Indicator />
  </Switch>
</div>
`;

export const variantDemo = String.raw`
<div className="switch-demo-stack">
  <Field className="switch-demo-field-row">
    <Field.Label>Seat alerts</Field.Label>
    <Switch defaultChecked variant="info">
      <Switch.Indicator />
    </Switch>
  </Field>
  <Field className="switch-demo-field-row">
    <Field.Label>Auto check-in</Field.Label>
    <Switch defaultChecked variant="success">
      <Switch.Indicator />
    </Switch>
  </Field>
</div>
`;

export const customIndicatorDemo = String.raw`
const CheckIcon = () => (
  <svg
    width="10"
    height="10"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth="3"
    strokeLinecap="round"
    strokeLinejoin="round"
  >
    <path d="M20 6 9 17l-5-5" />
  </svg>
);

render(
  <Field className="switch-demo-field-row">
    <Field.Label>Auto check-in</Field.Label>
    <Switch defaultChecked variant="success">
      <Switch.Indicator>
        {({ checked }) => (
          <span data-slot="thumb">{checked ? <CheckIcon /> : null}</span>
        )}
      </Switch.Indicator>
    </Switch>
  </Field>,
);
`;

export const renderPropDemo = String.raw`
<Field className="switch-demo-field-row">
  <Switch defaultChecked>
    {({ checked }) => (
      <>
        <Field.Label>
          {checked ? 'Operational alerts active' : 'Operational alerts paused'}
        </Field.Label>
        <Switch.Indicator />
      </>
    )}
  </Switch>
</Field>
`;

export const formDemo = String.raw`
function SwitchFormDemo() {
  const [saved, setSaved] = useState('Not submitted');

  return (
    <form
      className="switch-demo-form"
      onSubmit={(event) => {
        event.preventDefault();
        const data = new FormData(event.currentTarget);
        setSaved(
          [
            data.get('email') ? 'Email alerts' : null,
            data.get('sms') ? 'SMS alerts' : null,
          ]
            .filter(Boolean)
            .join(', ') || 'No alerts',
        );
      }}
    >
      <Field className="switch-demo-field-row">
        <Field.Label>Email alerts</Field.Label>
        <Switch name="email" value="enabled" defaultChecked>
          <Switch.Indicator />
        </Switch>
      </Field>
      <Field className="switch-demo-field-row">
        <Field.Label>SMS alerts</Field.Label>
        <Switch name="sms" value="enabled">
          <Switch.Indicator />
        </Switch>
      </Field>
      <div className="switch-demo-actions">
        <button className="switch-demo-button" type="submit">
          Save
        </button>
        <span className="switch-demo-status">{saved}</span>
      </div>
    </form>
  );
}

render(<SwitchFormDemo />);
`;

{/* prettier-ignore-end */}

# Switch

`Switch` lets users turn a single setting on or off, especially for preferences,
permissions, and form options. The root owns visual state (`size`, `variant`,
plus inherited `invalid` / `disabled` / `required` / `readOnly`),
`Switch.Indicator` is the track + sliding thumb. Wrap `Switch` in a `Field` to
attach a label, description, or error message — the field-level state cascades
into the switch automatically.

## Usage

```tsx
import { Field, Switch } from '@takeoff-ui/react-spar';
```

```tsx
<Field>
  <Field.Label />
  <Switch>
    <Switch.Indicator />
  </Switch>
  <Field.Description />
  <Field.ErrorMessage />
</Field>
```

## Playground

<LiveCode
  code={playgroundDemo}
  cssCode={switchDemoCss}
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Using with `Field`

`Field` is the generic ARIA wrapper. Setting `invalid`, `disabled`, `required`,
or `readOnly` on `Field` cascades into the nested `Switch`, and `Field.Label`,
`Field.Description`, and `Field.ErrorMessage` are wired to the switch through
shared IDs.

<LiveCode
  code={fieldWithSwitchDemo}
  cssCode={switchDemoCss}
  editable={false}
  previewMinHeight={460}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Controlled

<LiveCode
  code={controlledDemo}
  cssCode={switchDemoCss}
  editable={false}
  noInline
  previewMinHeight={240}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Sizes

<LiveCode
  code={sizeDemo}
  cssCode={switchDemoCss}
  editable={false}
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Variants

<LiveCode
  code={variantDemo}
  cssCode={switchDemoCss}
  editable={false}
  previewMinHeight={200}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Custom Indicator

`Switch.Indicator` accepts function children that receive the current `checked`
/ `disabled` / `readOnly` state, so you can render different content per state.
Passing a `ReactNode` instead replaces the default `thumb` slot entirely — the
consumer becomes responsible for any `data-slot="thumb"` wrapper they need for
theming.

<LiveCode
  code={customIndicatorDemo}
  cssCode={switchDemoCss}
  editable={false}
  noInline
  previewMinHeight={160}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Render Prop

<LiveCode
  code={renderPropDemo}
  cssCode={switchDemoCss}
  editable={false}
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Form Submission

When `name` is set, Spar renders a synchronized hidden checkbox input for native
form submission.

<LiveCode
  code={formDemo}
  cssCode={switchDemoCss}
  editable={false}
  noInline
  previewMinHeight={280}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Accessibility

- `Field.Label`, `Field.Description`, and `Field.ErrorMessage` are wired to the
  control via stable IDs derived from the `Field` root (`aria-labelledby`,
  `aria-describedby`, `aria-invalid`).
- `Switch` renders a focusable control with `role="switch"` and reflects its
  value through `aria-checked`.
- When the switch has no visible label (for example a size sampler), pass
  `aria-label` directly on `<Switch>`.
- Disabled switches are removed from the tab order. Read-only switches remain
  focusable and do not change value.
- When `name` is provided, Spar renders a synchronized hidden checkbox input for
  native form submission. `name` is the form field name, not the accessible
  label.

| Key                                 | Behavior                            |
| ----------------------------------- | ----------------------------------- |
| <kbd>Enter</kbd> / <kbd>Space</kbd> | Toggle the focused switch.          |
| <kbd>Tab</kbd>                      | Move focus to the next tabbable UI. |

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Switch {#switch}

See
[Spar Switch docs](https://spar.app.turkishtechlab.com/docs/Components/Switch)
for primitive behavior.

#### Props {#switch-props}

| Name                            | Type                                                                                    | Default | Description                                                                                                    |
| ------------------------------- | --------------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />   | <code>React.ReactNode &#124; ((state: SwitchRenderProps) =&gt; React.ReactNode)</code>  | -       | Compound children for switch anatomy, or a render function exposing Spar state.                                |
| <ApiBadge label="size" />       | <code>SwitchSize</code>                                                                 | 'base'  | Size scale.                                                                                                    |
| <ApiBadge label="variant" />    | <code>SwitchVariant</code>                                                              | 'info'  | Color variant used while checked.                                                                              |
| <ApiBadge label="invalid" />    | <code>boolean</code>                                                                    | false   | Marks the switch as visually invalid. Inherited from `<Field>` automatically; pass this prop only to override. |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;SwitchSlot, string&gt;&gt;</code>                            | -       | Per-slot class name overrides.                                                                                 |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;SwitchSlot, HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -       | Per-slot HTML attribute overrides.                                                                             |
| <ApiBadge label="className" />  | <code>string</code>                                                                     | -       | Appends custom classes to the root slot of this part.                                                          |

#### Data attributes {#switch-data-attributes}

| Attribute                | Applied when        | Purpose                                                                         |
| ------------------------ | ------------------- | ------------------------------------------------------------------------------- |
| `data-slot="root"`       | Always              | Stable selector for the root slot.                                              |
| `data-size`              | Always              | Reflects the resolved `size` prop so theme recipes can scope size variants.     |
| `data-variant`           | Always              | Reflects the resolved `variant` prop so theme recipes can scope color variants. |
| `data-state="checked"`   | When checked.       | Spar checked-state hook.                                                        |
| `data-state="unchecked"` | When unchecked.     | Spar unchecked-state hook.                                                      |
| `data-disabled`          | `disabled` is true. | Spar disabled-state hook.                                                       |
| `data-readonly`          | `readOnly` is true. | Spar read-only-state hook.                                                      |
| `data-required`          | `required` is true. | Spar required-state hook.                                                       |
| `data-invalid`           | `invalid` is true.  | Marks invalid visual state for theme recipes.                                   |

### Switch.Indicator {#switch-indicator}

See
[Spar Switch docs](https://spar.app.turkishtechlab.com/docs/Components/Switch)
for primitive behavior.

#### Data attributes {#switch-indicator-data-attributes}

| Attribute               | Applied when | Purpose                                 |
| ----------------------- | ------------ | --------------------------------------- |
| `data-slot="indicator"` | Always       | Stable selector for the indicator slot. |
| `data-slot="thumb"`     | Always       | Stable selector for the thumb slot.     |

{/* api-tables:end */}
