---
title: Switch
description:
  Lets users turn a single setting on or off, especially for preferences,
  permissions, and form options.
sidebar_label: Switch
sidebar_position: 2
slug: /components/switch
---

import LiveCode from '@site/src/components/LiveCode';
import {
  Badge,
  Button,
  Field,
  ReactSparDemoRoot,
  Switch,
} from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const switchScope = { Badge, Button, Field, Switch };

{/* prettier-ignore-start */}

export const playgroundDemo = String.raw`
function PlaygroundDemo() {
  return (
    <Field className="max-w-90">
      <div className="flex w-full items-center justify-between">
        <div>
          <Field.Label>Flight status alerts</Field.Label>
          <Field.Description>We will let you know about delays, gate changes, and cancellations.</Field.Description>
        </div>
        <Switch defaultChecked>
          <Switch.Indicator />
        </Switch>
      </div>
    </Field>
  );
}

render(<PlaygroundDemo />);
`;

export const fieldWithSwitchDemo = String.raw`
function FieldWithSwitchDemo() {
  return (
    <div className="grid w-full max-w-90 gap-6">
      <Field required>
        <div className="flex w-full items-center justify-between">
          <Field.Label>Email alerts</Field.Label>
          <Switch defaultChecked>
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>

      <Field>
        <div className="flex w-full items-center justify-between">
          <div>
            <Field.Label>SMS alerts</Field.Label>
            <Field.Description>Standard messaging rates may apply.</Field.Description>
          </div>
          <Switch>
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>

      <Field invalid>
        <div className="flex w-full items-center justify-between">
          <div>
            <Field.Label>Push notifications</Field.Label>
            <Field.ErrorMessage>Enable browser notifications to turn this on.</Field.ErrorMessage>
          </div>
          <Switch>
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>

      <Field disabled>
        <div className="flex w-full items-center justify-between">
          <Field.Label>Disabled setting</Field.Label>
          <Switch>
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>

      <Field readOnly>
        <div className="flex w-full items-center justify-between">
          <Field.Label>Read-only setting</Field.Label>
          <Switch defaultChecked>
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>
    </div>
  );
}

render(<FieldWithSwitchDemo />);
`;

export const controlledDemo = String.raw`
function ControlledSwitchDemo() {
  const [enabled, setEnabled] = useState(true);

  return (
    <div className="grid w-full max-w-90 gap-3">
      <Badge className="w-fit" variant="neutral" appearance="outlined">
        Notifications {enabled ? 'enabled' : 'paused'}
      </Badge>
      <Field>
        <div className="flex w-full items-center justify-between">
          <Field.Label>Flight status notifications</Field.Label>
          <Switch checked={enabled} onChange={setEnabled}>
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>
    </div>
  );
}

render(<ControlledSwitchDemo />);
`;

export const sizeDemo = String.raw`
function SizeDemo() {
  return (
    <div className="flex flex-wrap items-center gap-3">
      <Switch aria-label="Extra small switch" size="xsmall">
        <Switch.Indicator />
      </Switch>
      <Switch aria-label="Small switch" size="small">
        <Switch.Indicator />
      </Switch>
      <Switch aria-label="Base switch" size="base" defaultChecked>
        <Switch.Indicator />
      </Switch>
      <Switch aria-label="Large switch" size="large" defaultChecked>
        <Switch.Indicator />
      </Switch>
      <Switch aria-label="Extra large switch" size="xlarge" defaultChecked>
        <Switch.Indicator />
      </Switch>
    </div>
  );
}

render(<SizeDemo />);
`;

export const variantDemo = String.raw`
function VariantDemo() {
  return (
    <div className="grid w-full max-w-90 gap-3">
      <Field>
        <div className="flex w-full items-center justify-between">
          <Field.Label>Seat alerts</Field.Label>
          <Switch defaultChecked variant="info">
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>
      <Field>
        <div className="flex w-full items-center justify-between">
          <Field.Label>Auto check-in</Field.Label>
          <Switch defaultChecked variant="success">
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>
    </div>
  );
}

render(<VariantDemo />);
`;

export const customIndicatorDemo = String.raw`
function CustomIndicatorDemo() {
  const ToggleLabel = ({ active }) => (
    <span aria-hidden="true" className="inline-flex h-full w-full items-center justify-center whitespace-nowrap">
      <span className="text-[7px] font-semibold leading-none tracking-[0.08em] text-static-white">{active ? 'ON' : 'OFF'}</span>
    </span>
  );

  return (
    <Field className="max-w-90">
      <div className="flex w-full items-center justify-between">
        <Field.Label>Auto check-in</Field.Label>
        <Switch defaultChecked variant="success">
          <Switch.Indicator>
            {({ checked }) => (
              <span data-slot="thumb" className="inline-flex h-full w-full items-center justify-center">
                <ToggleLabel active={checked} />
              </span>
            )}
          </Switch.Indicator>
        </Switch>
      </div>
    </Field>
  );
}

render(<CustomIndicatorDemo />);
`;

export const renderPropDemo = String.raw`
function RenderPropDemo() {
  return (
    <div>
      <Field>
        <Switch defaultChecked>
          {({ checked }) => (
            <>
              <Field.Label>{checked ? 'Operational alerts active' : 'Operational alerts paused'}</Field.Label>
              <Switch.Indicator />
            </>
          )}
        </Switch>
      </Field>
    </div>
  );
}

render(<RenderPropDemo />);
`;

export const formDemo = String.raw`
function SwitchFormDemo() {
  const [saved, setSaved] = useState('Not submitted');

  return (
    <form
      className="grid w-full max-w-90 gap-3"
      onSubmit={(event) => {
        event.preventDefault();
        const data = new FormData(event.currentTarget);
        setSaved(
          [data.get('email') ? 'Email alerts' : null, data.get('sms') ? 'SMS alerts' : null].filter(Boolean).join(', ') || 'No alerts',
        );
      }}
    >
      <Field>
        <div className="flex w-full items-center justify-between">
          <Field.Label>Email alerts</Field.Label>
          <Switch name="email" value="enabled" defaultChecked>
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>
      <Field>
        <div className="flex w-full items-center justify-between">
          <Field.Label>SMS alerts</Field.Label>
          <Switch name="sms" value="enabled">
            <Switch.Indicator />
          </Switch>
        </div>
      </Field>
      <div className="flex flex-wrap items-center justify-end gap-3">
        <Button type="submit">Save</Button>
        <Badge className="w-fit" variant="neutral" appearance="outlined">
          {saved}
        </Badge>
      </div>
    </form>
  );
}

render(<SwitchFormDemo />);
`;

{/* prettier-ignore-end */}

# Switch

`Switch` lets users turn a single setting on or off, especially for preferences,
permissions, and form options. The root owns visual state (`size`, `variant`,
plus inherited `invalid` / `disabled` / `required` / `optional` / `readOnly`),
`Switch.Indicator` is the track + sliding thumb. Wrap `Switch` in a `Field` to
attach a label, description, or error message — the field-level state cascades
into the switch automatically.

## Usage

```tsx
import { Field, Switch } from '@takeoff-ui/react-spar';
```

```tsx
<Field>
  <Field.Label />
  <Switch>
    <Switch.Indicator />
  </Switch>
  <Field.Description />
  <Field.ErrorMessage />
</Field>
```

## Playground

<LiveCode
  code={playgroundDemo}
  noInline
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Using with `Field`

`Field` is the generic ARIA wrapper. Setting `invalid`, `disabled`, `required`,
`optional`, or `readOnly` on `Field` cascades into the nested `Switch`, and
`Field.Label`, `Field.Description`, and `Field.ErrorMessage` are wired to the
switch through shared IDs.

<LiveCode
  code={fieldWithSwitchDemo}
  noInline
  previewMinHeight={460}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Controlled

<LiveCode
  code={controlledDemo}
  noInline
  previewMinHeight={240}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Sizes

<LiveCode
  code={sizeDemo}
  noInline
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Variants

<LiveCode
  code={variantDemo}
  noInline
  previewMinHeight={200}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Custom Indicator

`Switch.Indicator` accepts function children that receive the current `checked`
/ `disabled` / `readOnly` state, so you can render different content per state.
Passing a `ReactNode` instead replaces the default `thumb` slot entirely — the
consumer becomes responsible for any `data-slot="thumb"` wrapper they need for
theming.

<LiveCode
  code={customIndicatorDemo}
  noInline
  previewMinHeight={160}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Render Prop

<LiveCode
  code={renderPropDemo}
  noInline
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Form Submission

When `name` is set, Spar renders a synchronized hidden checkbox input for native
form submission.

<LiveCode
  code={formDemo}
  noInline
  previewMinHeight={280}
  previewWrapper={ReactSparDemoRoot}
  scope={switchScope}
/>

## Accessibility

- `Field.Label`, `Field.Description`, and `Field.ErrorMessage` are wired to the
  control via stable IDs derived from the `Field` root (`aria-labelledby`,
  `aria-describedby`, `aria-invalid`).
- `Switch` renders a focusable control with `role="switch"` and reflects its
  value through `aria-checked`.
- When the switch has no visible label (for example a size sampler), pass
  `aria-label` directly on `<Switch>`.
- Disabled switches are removed from the tab order. Read-only switches remain
  focusable and do not change value.
- When `name` is provided, Spar renders a synchronized hidden checkbox input for
  native form submission. `name` is the form field name, not the accessible
  label.

| Key                                 | Behavior                            |
| ----------------------------------- | ----------------------------------- |
| <kbd>Enter</kbd> / <kbd>Space</kbd> | Toggle the focused switch.          |
| <kbd>Tab</kbd>                      | Move focus to the next tabbable UI. |

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Switch {#switch}

See
[Spar Switch docs](https://spar.app.turkishtechlab.com/docs/Components/Switch)
for primitive behavior.

#### Props {#switch-props}

| Name                            | Type                                                                                                                              | Default | Description                                                                                                    |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />   | <code>React.ReactNode &#124; ((state: <a href="#switch-td-switchrenderprops">SwitchRenderProps</a>) =&gt; React.ReactNode)</code> | -       | Compound children for switch anatomy, or a render function exposing Spar state.                                |
| <ApiBadge label="size" />       | <code><a href="#switch-td-switchsize">SwitchSize</a></code>                                                                       | 'base'  | Size scale.                                                                                                    |
| <ApiBadge label="variant" />    | <code><a href="#switch-td-switchvariant">SwitchVariant</a></code>                                                                 | 'info'  | Color variant used while checked.                                                                              |
| <ApiBadge label="invalid" />    | <code>boolean</code>                                                                                                              | false   | Marks the switch as visually invalid. Inherited from `<Field>` automatically; pass this prop only to override. |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;<a href="#switch-td-switchslot">SwitchSlot</a>, string&gt;&gt;</code>                                  | -       | Per-slot class name overrides.                                                                                 |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;<a href="#switch-td-switchslot">SwitchSlot</a>, React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -       | Per-slot HTML attribute overrides.                                                                             |
| <ApiBadge label="className" />  | <code>string</code>                                                                                                               | -       | Appends custom classes to the root slot of this part.                                                          |

#### Data attributes {#switch-data-attributes}

| Attribute                                             | Applied when        | Purpose                                                                         |
| ----------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" />       | Always              | Stable selector for the root slot.                                              |
| <ApiBadge label="data-size" />                        | Always              | Reflects the resolved `size` prop so theme recipes can scope size variants.     |
| <ApiBadge label="data-variant" />                     | Always              | Reflects the resolved `variant` prop so theme recipes can scope color variants. |
| <ApiBadge label="data-state=&quot;checked&quot;" />   | When checked.       | Spar checked-state hook.                                                        |
| <ApiBadge label="data-state=&quot;unchecked&quot;" /> | When unchecked.     | Spar unchecked-state hook.                                                      |
| <ApiBadge label="data-disabled" />                    | `disabled` is true. | Spar disabled-state hook.                                                       |
| <ApiBadge label="data-readonly" />                    | `readOnly` is true. | Spar read-only-state hook.                                                      |
| <ApiBadge label="data-required" />                    | `required` is true. | Spar required-state hook.                                                       |
| <ApiBadge label="data-invalid" />                     | `invalid` is true.  | Marks invalid visual state for theme recipes.                                   |

### Switch.Indicator {#switch-indicator}

See
[Spar Switch docs](https://spar.app.turkishtechlab.com/docs/Components/Switch)
for primitive behavior.

#### Data attributes {#switch-indicator-data-attributes}

| Attribute                                            | Applied when | Purpose                                 |
| ---------------------------------------------------- | ------------ | --------------------------------------- |
| <ApiBadge label="data-slot=&quot;indicator&quot;" /> | Always       | Stable selector for the indicator slot. |
| <ApiBadge label="data-slot=&quot;thumb&quot;" />     | Always       | Stable selector for the thumb slot.     |

### Type Definitions {#switch-type-definitions}

| Name                                                                                                   | Definition                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <span id="switch-td-switchrenderprops"><ApiBadge label="SwitchRenderProps" /></span>                   | <code>&#123; checked: boolean; setChecked: (checked: boolean) =&gt; void; disabled: boolean; readOnly: boolean; required: boolean; invalid: boolean; isFocused: boolean; isHovered: boolean; isPressed: boolean &#125;</code> |
| <span id="switch-td-switchsize"><ApiBadge label="SwitchSize" /></span>                                 | <code>'xlarge' &#124; 'large' &#124; 'base' &#124; 'small' &#124; 'xsmall'</code>                                                                                                                                             |
| <span id="switch-td-switchvariant"><ApiBadge label="SwitchVariant" /></span>                           | <code>'info' &#124; 'success'</code>                                                                                                                                                                                          |
| <span id="switch-td-switchslot"><ApiBadge label="SwitchSlot" /></span>                                 | <code>'root' &#124; 'indicator' &#124; 'thumb'</code>                                                                                                                                                                         |
| <span id="switch-td-switchindicatorrenderprops"><ApiBadge label="SwitchIndicatorRenderProps" /></span> | <code>&#123; checked: boolean; disabled: boolean; readOnly: boolean &#125;</code>                                                                                                                                             |

{/* api-tables:end */}
