---
title: Checkbox
description:
  Lets users select one or more options or toggle a single setting on or off,
  with support for an indeterminate mixed state.
sidebar_label: Checkbox
sidebar_position: 3
slug: /components/checkbox
---

import LiveCode from '@site/src/components/LiveCode';
import {
  Checkbox,
  Field,
  ReactSparDemoRoot,
} from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const checkboxScope = { Checkbox, Field };

{/* prettier-ignore-start */}

export const playgroundDemo = String.raw`
function PlaygroundDemo() {
  return (
    <Field>
      <Field.Label>Notifications</Field.Label>
      <Checkbox defaultChecked>
        <Checkbox.Indicator />
      </Checkbox>
      <Field.Description>
        We will only email you when a saved route drops in price.
      </Field.Description>
    </Field>
  );
}

render(<PlaygroundDemo />);
`;

export const fieldWithCheckboxDemo = String.raw`
function FieldWithCheckboxDemo() {
  return (
    <div className="grid max-w-130 gap-3.5">
      <Field required>
        <Field.Label>Email me booking updates</Field.Label>
        <Checkbox defaultChecked>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>

      <Field>
        <Field.Label>Subscribe to fare alerts</Field.Label>
        <Checkbox>
          <Checkbox.Indicator />
        </Checkbox>
        <Field.Description>
          We will only email you when a saved route drops in price.
        </Field.Description>
      </Field>

      <Field invalid>
        <Field.Label>I accept the booking terms</Field.Label>
        <Checkbox>
          <Checkbox.Indicator />
        </Checkbox>
        <Field.ErrorMessage>You must accept the terms to continue.</Field.ErrorMessage>
      </Field>

      <Field disabled>
        <Field.Label>Disabled option</Field.Label>
        <Checkbox>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>

      <Field readOnly>
        <Field.Label>Read-only selection</Field.Label>
        <Checkbox defaultChecked>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
    </div>
  );
}

render(<FieldWithCheckboxDemo />);
`;

export const controlledDemo = String.raw`
function ControlledCheckboxDemo() {
  const [accepted, setAccepted] = useState(false);

  return (
    <div className="grid max-w-130 gap-3.5">
      <div className="w-fit border border-border-light rounded-full bg-static-light text-text-dark text-[13px] font-semibold leading-none py-2 px-3">
        Terms {accepted ? 'accepted' : 'not accepted'}
      </div>
      <Field>
        <Field.Label>I accept the booking terms</Field.Label>
        <Checkbox checked={accepted} onChange={setAccepted}>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
    </div>
  );
}

render(<ControlledCheckboxDemo />);
`;

export const indeterminateDemo = String.raw`
function IndeterminateCheckboxDemo() {
  const [items, setItems] = useState({
    seat: true,
    meal: false,
    baggage: false,
  });

  const values = Object.values(items);
  const allChecked = values.every(Boolean);
  const noneChecked = values.every((v) => !v);
  const indeterminate = !allChecked && !noneChecked;

  const toggleAll = (next) => {
    setItems({ seat: next, meal: next, baggage: next });
  };

  return (
    <div className="grid max-w-130 gap-3.5">
      <Field>
        <Field.Label>All extras</Field.Label>
        <Checkbox
          checked={allChecked}
          indeterminate={indeterminate}
          onChange={toggleAll}
        >
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
      <div className="grid max-w-130 gap-3.5" style={{ paddingLeft: 24 }}>
        <Field>
          <Field.Label>Extra legroom seat</Field.Label>
          <Checkbox
            checked={items.seat}
            onChange={(v) => setItems({ ...items, seat: v })}
          >
            <Checkbox.Indicator />
          </Checkbox>
        </Field>
        <Field>
          <Field.Label>Special meal</Field.Label>
          <Checkbox
            checked={items.meal}
            onChange={(v) => setItems({ ...items, meal: v })}
          >
            <Checkbox.Indicator />
          </Checkbox>
        </Field>
        <Field>
          <Field.Label>Extra baggage</Field.Label>
          <Checkbox
            checked={items.baggage}
            onChange={(v) => setItems({ ...items, baggage: v })}
          >
            <Checkbox.Indicator />
          </Checkbox>
        </Field>
      </div>
    </div>
  );
}

render(<IndeterminateCheckboxDemo />);
`;

export const sizeDemo = String.raw`
function SizeDemo() {
  return (
    <div className="flex flex-wrap gap-4.5">
      <Field>
        <Field.Label>Small</Field.Label>
        <Checkbox size="small" defaultChecked>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
      <Field>
        <Field.Label>Base</Field.Label>
        <Checkbox size="base" defaultChecked>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
    </div>
  );
}

render(<SizeDemo />);
`;

export const cardTypeDemo = String.raw`
function CardTypeDemo() {
  return (
    <div className="grid max-w-130 gap-3.5">
      <Field>
        <Field.Label>Mobile boarding pass</Field.Label>
        <Checkbox type="card" defaultChecked>
          <Checkbox.Indicator />
        </Checkbox>
        <Field.Description>
          Skip the printed pass and board with your phone.
        </Field.Description>
      </Field>
      <Field>
        <Field.Label>Priority boarding</Field.Label>
        <Checkbox type="card">
          <Checkbox.Indicator />
        </Checkbox>
        <Field.Description>
          Board before general groups with priority access.
        </Field.Description>
      </Field>
    </div>
  );
}

render(<CardTypeDemo />);
`;

export const customIconDemo = String.raw`
function CustomIconDemo() {
  const StarIcon = () => (
    <svg
      width="14"
      height="14"
      viewBox="0 0 24 24"
      fill="currentColor"
    >
      <path d="M12 2 15 9l7 .5-5.5 4.5L18 22l-6-4-6 4 1.5-8L2 9.5 9 9z" />
    </svg>
  );

  const DashIcon = () => (
    <svg
      width="14"
      height="14"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="3"
      strokeLinecap="round"
    >
      <path d="M5 12h14" />
    </svg>
  );

  return (
    <div className="grid max-w-130 gap-3.5">
      <Field>
        <Field.Label>Favorite this route</Field.Label>
        <Checkbox defaultChecked>
          <Checkbox.Indicator>
            {({ checked, indeterminate }) =>
              indeterminate ? <DashIcon /> : checked ? <StarIcon /> : null
            }
          </Checkbox.Indicator>
        </Checkbox>
      </Field>
    </div>
  );
}

render(<CustomIconDemo />);
`;

export const stateDemo = String.raw`
function StateDemo() {
  return (
    <div className="grid max-w-130 gap-3.5">
      <Field disabled>
        <Field.Label>Disabled option</Field.Label>
        <Checkbox>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
      <Field disabled>
        <Field.Label>Disabled and selected</Field.Label>
        <Checkbox defaultChecked>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
      <Field readOnly>
        <Field.Label>Read-only selection</Field.Label>
        <Checkbox defaultChecked>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
      <Field invalid required>
        <Field.Label>I accept the booking terms</Field.Label>
        <Checkbox>
          <Checkbox.Indicator />
        </Checkbox>
        <Field.ErrorMessage>
          You must accept the terms to continue.
        </Field.ErrorMessage>
      </Field>
    </div>
  );
}

render(<StateDemo />);
`;

export const formDemo = String.raw`
function CheckboxFormDemo() {
  const [saved, setSaved] = useState('Not submitted');

  return (
    <form
      className="grid max-w-130 gap-3.5"
      onSubmit={(event) => {
        event.preventDefault();
        const data = new FormData(event.currentTarget);
        const picked = data.getAll('extras');
        setSaved(picked.length ? picked.join(', ') : 'No extras');
      }}
    >
      <Field>
        <Field.Label>Extra legroom seat</Field.Label>
        <Checkbox name="extras" value="seat" defaultChecked>
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
      <Field>
        <Field.Label>Special meal</Field.Label>
        <Checkbox name="extras" value="meal">
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
      <Field>
        <Field.Label>Extra baggage</Field.Label>
        <Checkbox name="extras" value="baggage">
          <Checkbox.Indicator />
        </Checkbox>
      </Field>
      <div className="flex flex-wrap items-center gap-3">
     <button className="items-center min-h-10 px-2.5 py-2 rounded-lg bg-primary-base text-static-white text-sm cursor-pointer" type="submit">
          Save
        </button>
        <span className="w-fit border border-border-light rounded-full bg-static-light text-text-dark text-[13px] font-semibold leading-none py-2 px-3">{saved}</span>
      </div>
    </form>
  );
}

render(<CheckboxFormDemo />);
`;

{/* prettier-ignore-end */}

# Checkbox

`Checkbox` is a compound, accessible toggle control. The root owns visual state
(`size`, `type`, plus inherited `invalid` / `disabled` / `required` /
`readOnly`), `Checkbox.Indicator` is the bordered box that hosts the icon. Wrap
`Checkbox` in a `Field` to attach a label, description, or error message — the
field-level state cascades into the checkbox automatically.

## Usage

```tsx
import { Checkbox, Field } from '@takeoff-ui/react-spar';
```

```tsx
<Field>
  <Field.Label />
  <Checkbox>
    <Checkbox.Indicator />
  </Checkbox>
  <Field.Description />
  <Field.ErrorMessage />
</Field>
```

## Playground

<LiveCode
  code={playgroundDemo}
  noInline
  previewMinHeight={160}
  previewWrapper={ReactSparDemoRoot}
  scope={checkboxScope}
/>

## Using with `Field`

`Field` is the generic ARIA wrapper. Setting `invalid`, `disabled`, `required`,
or `readOnly` on `Field` cascades into the nested `Checkbox`, and `Field.Label`,
`Field.Description`, and `Field.ErrorMessage` are wired to the field control
through shared IDs.

<LiveCode
  code={fieldWithCheckboxDemo}
  noInline
  previewMinHeight={460}
  previewWrapper={ReactSparDemoRoot}
  scope={checkboxScope}
/>

## Controlled

<LiveCode
  code={controlledDemo}
  noInline
  previewMinHeight={200}
  previewWrapper={ReactSparDemoRoot}
  scope={checkboxScope}
/>

## Indeterminate

`indeterminate` overrides `checked` and `defaultChecked` while set, and emits
`aria-checked="mixed"`. The first user toggle transitions out of the mixed state
— `onChange` is always called with a plain `boolean`. In uncontrolled mode, set
`indeterminate` only on the initial render and clear it from your own state
after the first change.

<LiveCode
  code={indeterminateDemo}
  noInline
  previewMinHeight={260}
  previewWrapper={ReactSparDemoRoot}
  scope={checkboxScope}
/>

## Sizes

<LiveCode
  code={sizeDemo}
  noInline
  previewMinHeight={120}
  previewWrapper={ReactSparDemoRoot}
  scope={checkboxScope}
/>

## Card Type

Set `type="card"` to switch to the bordered card variant for option-row layouts.

<LiveCode
  code={cardTypeDemo}
  noInline
  previewMinHeight={220}
  previewWrapper={ReactSparDemoRoot}
  scope={checkboxScope}
/>

## Custom Icon

`Checkbox.Indicator` accepts function children that receive the current
`checked` and `indeterminate` state, so you can render different glyphs per
state without mounting two icons. Passing a `ReactNode` instead replaces the
default `icon` slot entirely.

<LiveCode
  code={customIconDemo}
  noInline
  previewMinHeight={160}
  previewWrapper={ReactSparDemoRoot}
  scope={checkboxScope}
/>

## Disabled, Read-Only, and Invalid

<LiveCode
  code={stateDemo}
  noInline
  previewMinHeight={260}
  previewWrapper={ReactSparDemoRoot}
  scope={checkboxScope}
/>

## Form Submission

When `name` is set, Spar renders a synchronized hidden checkbox input for native
form submission. Multiple checkboxes sharing the same `name` produce an array
via `FormData.getAll`.

<LiveCode
  code={formDemo}
  noInline
  previewMinHeight={300}
  previewWrapper={ReactSparDemoRoot}
  scope={checkboxScope}
/>

## Accessibility

- `Field.Label`, `Field.Description`, and `Field.ErrorMessage` are wired to the
  control via stable IDs derived from the `Field` root (`aria-labelledby`,
  `aria-describedby`, `aria-invalid`).
- `Checkbox` renders a focusable control with `role="checkbox"` and reflects its
  value through `aria-checked` (including `"mixed"` for the indeterminate
  state).
- The asterisk inside `Field.Label` is decorative — `required` is also surfaced
  to assistive tech via the input's native `required` / `aria-required`.
- Disabled checkboxes are removed from the tab order. Read-only checkboxes
  remain focusable and do not change value.

| Key              | Behavior                            |
| ---------------- | ----------------------------------- |
| <kbd>Space</kbd> | Toggle the focused checkbox.        |
| <kbd>Tab</kbd>   | Move focus to the next tabbable UI. |

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Checkbox {#checkbox}

See
[Spar Checkbox docs](https://spar.app.turkishtechlab.com/docs/Components/Checkbox)
for primitive behavior.

#### Props {#checkbox-props}

| Name                               | Type                                                                                                                                    | Default   | Description                                                                                                         |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />      | <code>React.ReactNode &#124; ((state: <a href="#checkbox-td-checkboxrenderprops">CheckboxRenderProps</a>) =&gt; React.ReactNode)</code> | -         | Compound children for checkbox anatomy, or a render function exposing Spar tri-state.                               |
| <ApiBadge label="indeterminate" /> | <code>boolean</code>                                                                                                                    | false     | Indeterminate (mixed) visual + ARIA state. Overrides `checked` / `defaultChecked` and emits `aria-checked="mixed"`. |
| <ApiBadge label="invalid" />       | <code>boolean</code>                                                                                                                    | false     | Marks the checkbox as visually invalid.                                                                             |
| <ApiBadge label="size" />          | <code><a href="#checkbox-td-checkboxsize">CheckboxSize</a></code>                                                                       | 'base'    | Size scale.                                                                                                         |
| <ApiBadge label="type" />          | <code><a href="#checkbox-td-checkboxtype">CheckboxType</a></code>                                                                       | 'default' | Visual appearance. `card` switches to the bordered card variant.                                                    |
| <ApiBadge label="classNames" />    | <code>Partial&lt;Record&lt;<a href="#checkbox-td-checkboxslot">CheckboxSlot</a>, string&gt;&gt;</code>                                  | -         | Per-slot class name overrides.                                                                                      |
| <ApiBadge label="slotProps" />     | <code>Partial&lt;Record&lt;<a href="#checkbox-td-checkboxslot">CheckboxSlot</a>, HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code>       | -         | Per-slot HTML attribute overrides.                                                                                  |
| <ApiBadge label="className" />     | <code>string</code>                                                                                                                     | -         | Appends custom classes to the root slot of this part.                                                               |

#### Data attributes {#checkbox-data-attributes}

| Attribute                                       | Applied when        | Purpose                                                                     |
| ----------------------------------------------- | ------------------- | --------------------------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always              | Stable selector for the root slot.                                          |
| <ApiBadge label="data-size" />                  | Always              | Reflects the resolved `size` prop so theme recipes can scope size variants. |
| <ApiBadge label="data-type" />                  | Always              | Reflects the resolved `type` prop so theme recipes can scope type variants. |
| <ApiBadge label="data-invalid" />               | `invalid` is true.  | Marks invalid visual state for theme recipes.                               |
| <ApiBadge label="data-checked" />               | When checked.       | Spar checked-state hook.                                                    |
| <ApiBadge label="data-indeterminate" />         | When indeterminate. | Spar indeterminate-state hook.                                              |
| <ApiBadge label="data-disabled" />              | `disabled` is true. | Spar disabled-state hook.                                                   |
| <ApiBadge label="data-readonly" />              | `readOnly` is true. | Spar read-only-state hook.                                                  |
| <ApiBadge label="data-required" />              | `required` is true. | Spar required-state hook.                                                   |

### Checkbox.Indicator {#checkbox-indicator}

See
[Spar Checkbox docs](https://spar.app.turkishtechlab.com/docs/Components/Checkbox)
for primitive behavior.

#### Data attributes {#checkbox-indicator-data-attributes}

| Attribute                                            | Applied when | Purpose                                 |
| ---------------------------------------------------- | ------------ | --------------------------------------- |
| <ApiBadge label="data-slot=&quot;indicator&quot;" /> | Always       | Stable selector for the indicator slot. |
| <ApiBadge label="data-slot=&quot;icon&quot;" />      | Always       | Stable selector for the icon slot.      |

### Type Definitions {#checkbox-type-definitions}

| Name                                                                                                         | Definition                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <span id="checkbox-td-checkboxrenderprops"><ApiBadge label="CheckboxRenderProps" /></span>                   | <code>&#123; checked: CheckedState; setChecked: (checked: CheckedState) =&gt; void; disabled: boolean; readOnly: boolean; required: boolean; invalid: boolean; isFocused: boolean; isHovered: boolean; isPressed: boolean &#125;</code> |
| <span id="checkbox-td-checkboxsize"><ApiBadge label="CheckboxSize" /></span>                                 | <code>'small' &#124; 'base'</code>                                                                                                                                                                                                      |
| <span id="checkbox-td-checkboxtype"><ApiBadge label="CheckboxType" /></span>                                 | <code>'default' &#124; 'card'</code>                                                                                                                                                                                                    |
| <span id="checkbox-td-checkboxslot"><ApiBadge label="CheckboxSlot" /></span>                                 | <code>'root' &#124; 'indicator' &#124; 'icon'</code>                                                                                                                                                                                    |
| <span id="checkbox-td-checkboxindicatorrenderprops"><ApiBadge label="CheckboxIndicatorRenderProps" /></span> | <code>&#123; checked: boolean; indeterminate: boolean &#125;</code>                                                                                                                                                                     |

{/* api-tables:end */}
