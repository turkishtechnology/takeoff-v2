---
title: Select
description:
  Accessible custom select with controlled / uncontrolled state, keyboard
  typeahead, and grouped options.
sidebar_label: Select
sidebar_position: 6
slug: /components/select
---

import LiveCode from '@site/src/components/LiveCode';
import {
  Badge,
  Field,
  Select,
  ReactSparDemoRoot,
} from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const selectScope = { Badge, Field, Select };

{/* prettier-ignore-start */}

export const playgroundDemo = String.raw`
function PlaygroundDemo() {
  return (
    <Field className="max-w-90" required>
      <Field.Label>Cabin</Field.Label>
      <Select defaultValue="economy">
        <Select.Trigger placeholder="Pick a cabin" />
        <Select.Content>
          <Select.Item value="economy" label="Economy">
            Economy
          </Select.Item>
          <Select.Item value="business" label="Business">
            Business
          </Select.Item>
          <Select.Item value="first" label="First class">
            First class
          </Select.Item>
        </Select.Content>
      </Select>
      <Field.Description>Select the cabin for this itinerary.</Field.Description>
    </Field>
  );
}

render(<PlaygroundDemo />);
`;

export const sizesDemo = String.raw`
function SizesDemo() {
  return (
    <div className="grid w-full max-w-90 gap-3">
      <Select size="small">
        <Select.Trigger placeholder="Small" />
        <Select.Content>
          <Select.Item value="ist" label="Istanbul">
            Istanbul
          </Select.Item>
          <Select.Item value="ank" label="Ankara">
            Ankara
          </Select.Item>
        </Select.Content>
      </Select>
      <Select size="base">
        <Select.Trigger placeholder="Base" />
        <Select.Content>
          <Select.Item value="ist" label="Istanbul">
            Istanbul
          </Select.Item>
          <Select.Item value="ank" label="Ankara">
            Ankara
          </Select.Item>
        </Select.Content>
      </Select>
      <Select size="large">
        <Select.Trigger placeholder="Large" />
        <Select.Content>
          <Select.Item value="ist" label="Istanbul">
            Istanbul
          </Select.Item>
          <Select.Item value="ank" label="Ankara">
            Ankara
          </Select.Item>
        </Select.Content>
      </Select>
    </div>
  );
}

render(<SizesDemo />);
`;

export const groupsDemo = String.raw`
function GroupsDemo() {
  return (
    <Select className="max-w-90" defaultValue="ist">
      <Select.Trigger placeholder="Choose origin" />
      <Select.Content>
        <Select.Group>
          <Select.Label>Türkiye</Select.Label>
          <Select.Item value="ist" label="Istanbul">
            Istanbul
          </Select.Item>
          <Select.Item value="ank" label="Ankara">
            Ankara
          </Select.Item>
          <Select.Item value="izm" label="Izmir">
            Izmir
          </Select.Item>
        </Select.Group>
        <Select.Separator />
        <Select.Group>
          <Select.Label>Europe</Select.Label>
          <Select.Item value="lhr" label="London Heathrow">
            London Heathrow
          </Select.Item>
          <Select.Item value="cdg" label="Paris CDG">
            Paris CDG
          </Select.Item>
          <Select.Item value="fra" disabled label="Frankfurt (unavailable)">
            Frankfurt (unavailable)
          </Select.Item>
        </Select.Group>
      </Select.Content>
    </Select>
  );
}

render(<GroupsDemo />);
`;

export const indicatorDemo = String.raw`
function IndicatorDemo() {
  const PlusMinus = ({ isOpen }) => (
    <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
      <path d="M3 8h10" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
      {!isOpen && <path d="M8 3v10" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />}
    </svg>
  );

  return (
    <div className="grid w-full max-w-90 gap-3">
      {/* Default — a chevron that flips direction with the open state. */}
      <Select defaultValue="ist">
        <Select.Trigger placeholder="Default chevron" />
        <Select.Content>
          <Select.Item value="ist" label="Istanbul">Istanbul</Select.Item>
          <Select.Item value="ank" label="Ankara">Ankara</Select.Item>
        </Select.Content>
      </Select>

      {/* Custom indicator via the Trigger prop — render function gets { isOpen }. */}
      <Select defaultValue="ist">
        <Select.Trigger placeholder="Custom icon" indicator={(state) => <PlusMinus isOpen={state.isOpen} />} />
        <Select.Content>
          <Select.Item value="ist" label="Istanbul">Istanbul</Select.Item>
          <Select.Item value="ank" label="Ankara">Ankara</Select.Item>
        </Select.Content>
      </Select>

      {/* Opt out entirely. */}
      <Select defaultValue="ist">
        <Select.Trigger placeholder="No indicator" indicator={false} />
        <Select.Content>
          <Select.Item value="ist" label="Istanbul">Istanbul</Select.Item>
          <Select.Item value="ank" label="Ankara">Ankara</Select.Item>
        </Select.Content>
      </Select>
    </div>
  );
}

render(<IndicatorDemo />);
`;

export const indicatorCompoundDemo = String.raw`
function IndicatorCompoundDemo() {
  // Full layout control: a render-prop children opts the trigger out of the
  // built-in indicator, so you own every node — drop a standalone
  // <Select.Indicator /> wherever you want it.
  return (
    <Select className="max-w-90" defaultValue="ist">
      <Select.Trigger>
        {({ label }) => (
          <>
            <span className="tk-select-value">{label || 'Choose a city'}</span>
            <Select.Indicator>
              {({ isOpen }) => (isOpen ? '▲' : '▼')}
            </Select.Indicator>
          </>
        )}
      </Select.Trigger>
      <Select.Content>
        <Select.Item value="ist" label="Istanbul">Istanbul</Select.Item>
        <Select.Item value="ank" label="Ankara">Ankara</Select.Item>
        <Select.Item value="izm" label="Izmir">Izmir</Select.Item>
      </Select.Content>
    </Select>
  );
}

render(<IndicatorCompoundDemo />);
`;

export const controlledDemo = String.raw`
function ControlledSelectDemo() {
  const [value, setValue] = useState('eco');

  return (
    <div className="grid w-full max-w-90 gap-3">
      <Badge className="w-fit" variant="neutral" appearance="outlined">
        Selected: {value}
      </Badge>
      <Select value={value} onChange={setValue}>
        <Select.Trigger placeholder="Pick a fare" />
        <Select.Content>
          <Select.Item value="eco" label="Eco">
            Eco
          </Select.Item>
          <Select.Item value="flex" label="Eco Flex">
            Eco Flex
          </Select.Item>
          <Select.Item value="biz" label="Business">
            Business
          </Select.Item>
        </Select.Content>
      </Select>
    </div>
  );
}

render(<ControlledSelectDemo />);
`;

export const invalidDemo = String.raw`
function InvalidDemo() {
  return (
    <Select className="max-w-90" invalid>
      <Select.Trigger placeholder="Required field" />
      <Select.Content>
        <Select.Item value="a" label="Option A">
          Option A
        </Select.Item>
        <Select.Item value="b" label="Option B">
          Option B
        </Select.Item>
      </Select.Content>
    </Select>
  );
}

render(<InvalidDemo />);
`;

export const fieldDemo = String.raw`
function FieldSelectDemo() {
  return (
    <Field className="max-w-90" required>
      <Field.Label>Cabin</Field.Label>
      <Select invalid>
        <Select.Trigger placeholder="Choose cabin" aria-invalid="true" />
        <Select.Content>
          <Select.Item value="economy" label="Economy">
            Economy
          </Select.Item>
          <Select.Item value="business" label="Business">
            Business
          </Select.Item>
          <Select.Item value="first" label="First class">
            First class
          </Select.Item>
        </Select.Content>
      </Select>
      <Field.Description>Select the cabin for this itinerary.</Field.Description>
      <Field.ErrorMessage>Please choose a cabin to continue.</Field.ErrorMessage>
    </Field>
  );
}

render(<FieldSelectDemo />);
`;

{/* prettier-ignore-end */}

# Select

`Select` is a fully-keyboard, accessible custom select. State (value, open
state, disabled, required) lives on the root; the trigger renders the collapsed
button, and the content portals a positioned dropdown. Wrap `Select` in a
`Field` to attach a label, helper text, or error message — the field-level state
cascades into the trigger automatically.

## Usage

```tsx
import { Field, Select } from '@takeoff-ui/react-spar';
```

```tsx
<Field>
  <Field.Label />
  <Select>
    <Select.Trigger placeholder="Choose..." />
    <Select.Content>
      <Select.Group>
        <Select.Label />
        <Select.Item value="..." label="...">
          ...
        </Select.Item>
      </Select.Group>
      <Select.Separator />
    </Select.Content>
  </Select>
  <Field.Description />
  <Field.ErrorMessage />
</Field>
```

## Playground

<LiveCode
  code={playgroundDemo}
  noInline
  previewMinHeight={220}
  previewWrapper={ReactSparDemoRoot}
  scope={selectScope}
/>

## Using with `Field`

`Field` is the generic ARIA wrapper. Setting `invalid`, `disabled`, `required`,
`optional`, or `readOnly` on `Field` cascades into the nested `Select`, and
`Field.Label`, `Field.Description`, and `Field.ErrorMessage` are wired to the
trigger through shared IDs.

<LiveCode
  code={fieldDemo}
  noInline
  previewMinHeight={220}
  previewWrapper={ReactSparDemoRoot}
  scope={selectScope}
/>

## Sizes

<LiveCode
  code={sizesDemo}
  noInline
  previewMinHeight={340}
  previewWrapper={ReactSparDemoRoot}
  scope={selectScope}
/>

## Groups, Separator, Disabled Item

<LiveCode
  code={groupsDemo}
  noInline
  previewMinHeight={260}
  previewWrapper={ReactSparDemoRoot}
  scope={selectScope}
/>

## Indicator

`Select.Trigger` renders a disclosure chevron at its trailing edge by default —
it flips direction (and switches to the primary color) with the open state, the
same visual language as [`Accordion.Indicator`](/docs/components/accordion).
Pass the `indicator` prop to customize it: a node for a custom static icon, a
render function `({ isOpen }) => …` to swap icons by open state, or `false` to
hide it.

<LiveCode
  code={indicatorDemo}
  noInline
  previewMinHeight={220}
  previewWrapper={ReactSparDemoRoot}
  scope={selectScope}
/>

For full layout control, drive the trigger's render-prop `children`. A
render-function `children` opts the trigger out of its built-in indicator and
value wrapper entirely — you own every node, so place a standalone
`<Select.Indicator />` wherever you need it. Its `children` accept the same
node-or-render-function form, so you can swap the glyph by `isOpen` without
reaching for a hook.

<LiveCode
  code={indicatorCompoundDemo}
  noInline
  previewMinHeight={140}
  previewWrapper={ReactSparDemoRoot}
  scope={selectScope}
/>

## Controlled

<LiveCode
  code={controlledDemo}
  noInline
  previewMinHeight={220}
  previewWrapper={ReactSparDemoRoot}
  scope={selectScope}
/>

## Invalid State

<LiveCode
  code={invalidDemo}
  noInline
  previewMinHeight={140}
  previewWrapper={ReactSparDemoRoot}
  scope={selectScope}
/>

## Accessibility & Keyboard

- `Select.Trigger` renders a button with `role="combobox"`,
  `aria-haspopup="listbox"`, and `aria-expanded`.
- `Select.Content` is a `role="listbox"` portalled to `document.body` by default
  and positioned with Floating UI.
- The trigger displays the selected item's `label` (or the configured
  `placeholder`) and exposes that as its accessible name; when nested in a
  `Field` it also gets `aria-labelledby` pointing at the Field label.

| Key                                 | Behavior                                                     |
| ----------------------------------- | ------------------------------------------------------------ |
| <kbd>Enter</kbd> / <kbd>Space</kbd> | Open the dropdown or select the highlighted item.            |
| <kbd>↓</kbd> / <kbd>↑</kbd>         | Open the dropdown / move highlight to next or previous item. |
| <kbd>Home</kbd> / <kbd>End</kbd>    | Move highlight to the first / last item.                     |
| <kbd>Esc</kbd>                      | Close the dropdown without changing selection.               |
| typing characters                   | Typeahead — jumps to the first matching item.                |

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Select {#select}

See
[Spar Select docs](https://spar.app.turkishtechlab.com/docs/Components/Select)
for primitive behavior.

#### Props {#select-props}

| Name                              | Type                                                                                      | Default   | Description                                                                                                                                                      |
| --------------------------------- | ----------------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />     | <code>React.ReactNode</code>                                                              | -         | Compound parts (`Select.Trigger`, `Select.Content`).                                                                                                             |
| <ApiBadge label="size" />         | <code><a href="#select-td-selectsize">SelectSize</a></code>                               | 'base'    | Size scale.                                                                                                                                                      |
| <ApiBadge label="invalid" />      | <code>boolean</code>                                                                      | false     | Invalid state for form validation styling.                                                                                                                       |
| <ApiBadge label="contentWidth" /> | <code><a href="#select-td-selectcontentwidth">SelectContentWidth</a></code>               | 'trigger' | How the portalled SelectContent panel computes its width. See `SelectContentWidth`.                                                                              |
| <ApiBadge label="classNames" />   | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                                  | -         | Per-slot class name overrides.                                                                                                                                   |
| <ApiBadge label="slotProps" />    | <code>Partial&lt;Record&lt;"root", React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -         | Per-slot HTML attribute overrides.                                                                                                                               |
| <ApiBadge label="defaultValue" /> | <code>string</code>                                                                       | -         | Uncontrolled initial value                                                                                                                                       |
| <ApiBadge label="autoFocus" />    | <code>boolean</code>                                                                      | false     | Whether to focus the trigger on mount                                                                                                                            |
| <ApiBadge label="id" />           | <code>string</code>                                                                       | -         | Custom base ID for ARIA relationships. If not provided, one will be generated automatically. Sub-element IDs are derived as `${id}-trigger` and `${id}-content`. |
| <ApiBadge label="value" />        | <code>string</code>                                                                       | -         | Controlled selected value                                                                                                                                        |
| <ApiBadge label="disabled" />     | <code>boolean</code>                                                                      | -         | Disables the entire select. When inside a Field, inherited from Field.                                                                                           |
| <ApiBadge label="name" />         | <code>string</code>                                                                       | -         | Form field name                                                                                                                                                  |
| <ApiBadge label="required" />     | <code>boolean</code>                                                                      | -         | Makes the select required for forms. When inside a Field, inherited from Field.                                                                                  |
| <ApiBadge label="readOnly" />     | <code>boolean</code>                                                                      | -         | Select read-only state. When inside a Field, inherited from Field. A read-only select can be opened and inspected but its value cannot change.                   |
| <ApiBadge label="open" />         | <code>boolean</code>                                                                      | -         | Controlled open state                                                                                                                                            |
| <ApiBadge label="defaultOpen" />  | <code>boolean</code>                                                                      | false     | Uncontrolled initial open state                                                                                                                                  |
| <ApiBadge label="className" />    | <code>string</code>                                                                       | -         | Appends custom classes to the root slot of this part.                                                                                                            |

#### Events {#select-events}

| Name                              | Type                                    | Default | Description                      |
| --------------------------------- | --------------------------------------- | ------- | -------------------------------- |
| <ApiBadge label="onChange" />     | <code>(value: string) =&gt; void</code> | -       | Callback when selection changes  |
| <ApiBadge label="onOpenChange" /> | <code>(open: boolean) =&gt; void</code> | -       | Callback when open state changes |

#### Data attributes {#select-data-attributes}

| Attribute                                       | Applied when             | Purpose                                                                     |
| ----------------------------------------------- | ------------------------ | --------------------------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always                   | Stable selector for wrapper styling on the root slot.                       |
| <ApiBadge label="data-size" />                  | Always                   | Reflects the resolved `size` prop so theme recipes can scope size variants. |
| <ApiBadge label="data-invalid" />               | When `invalid` is true.  | Borders/background switch to the danger state. Mirrored from the root.      |
| <ApiBadge label="data-disabled" />              | When `disabled` is true. | Theme hook for the disabled state. Emitted by Spar Select.                  |

### Select.Trigger {#select-trigger}

See
[Spar Select docs](https://spar.app.turkishtechlab.com/docs/Components/Select)
for primitive behavior.

#### Props {#select-trigger-props}

| Name                             | Type                                                                                                                                                               | Default | Description                                                                                                                                                                                                                 |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />    | <code>React.ReactNode</code>                                                                                                                                       | -       | Trigger content. Defaults to the selected item label or `placeholder`; supply children to override the rendering, or pass a render function for full layout control.                                                        |
| <ApiBadge label="indicator" />   | <code>boolean &#124; React.ReactNode &#124; ((state: <a href="#select-td-selectindicatorrenderstate">SelectIndicatorRenderState</a>) =&gt; React.ReactNode)</code> | true    | Disclosure indicator after the value. Defaults to a chevron that flips with the open state. Pass `false` to hide it, a node for a custom static icon, or a render function `({ isOpen }) => …` to swap icons by open state. |
| <ApiBadge label="classNames" />  | <code>Partial&lt;Record&lt;<a href="#select-td-selecttriggerslot">SelectTriggerSlot</a>, string&gt;&gt;</code>                                                     | -       | Per-slot class name overrides.                                                                                                                                                                                              |
| <ApiBadge label="slotProps" />   | <code>Partial&lt;Record&lt;<a href="#select-td-selecttriggerslot">SelectTriggerSlot</a>, React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code>                    | -       | Per-slot HTML attribute overrides.                                                                                                                                                                                          |
| <ApiBadge label="placeholder" /> | <code>React.ReactNode</code>                                                                                                                                       | -       | Text shown when no value is selected                                                                                                                                                                                        |
| <ApiBadge label="className" />   | <code>string</code>                                                                                                                                                | -       | Appends custom classes to the root slot of this part.                                                                                                                                                                       |

#### Data attributes {#select-trigger-data-attributes}

| Attribute                                          | Applied when                  | Purpose                                                                     |
| -------------------------------------------------- | ----------------------------- | --------------------------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" />    | Always                        | Stable selector for wrapper styling on the root slot.                       |
| <ApiBadge label="data-size" />                     | Always                        | Reflects the resolved `size` prop so theme recipes can scope size variants. |
| <ApiBadge label="data-invalid" />                  | When `invalid` is true.       | Borders/background switch to the danger state. Mirrored from the root.      |
| <ApiBadge label="data-state=&quot;open&quot;" />   | While the dropdown is open.   | Spar open-state hook on the trigger and content.                            |
| <ApiBadge label="data-state=&quot;closed&quot;" /> | While the dropdown is closed. | Spar closed-state hook on the trigger.                                      |
| <ApiBadge label="data-placeholder" />              | When no value is selected.    | Theme hook to style the placeholder color.                                  |

### Select.Indicator {#select-indicator}

See
[Spar Select docs](https://spar.app.turkishtechlab.com/docs/Components/Select)
for primitive behavior.

#### Data attributes {#select-indicator-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |

### Select.Content {#select-content}

See
[Spar Select docs](https://spar.app.turkishtechlab.com/docs/Components/Select)
for primitive behavior.

#### Props {#select-content-props}

| Name                            | Type                                                                                      | Default       | Description                                                                |
| ------------------------------- | ----------------------------------------------------------------------------------------- | ------------- | -------------------------------------------------------------------------- |
| <ApiBadge label="children" />   | <code>React.ReactNode</code>                                                              | -             | `Select.Item`, `Select.Group`, `Select.Separator`.                         |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                                  | -             | Per-slot class name overrides.                                             |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -             | Per-slot HTML attribute overrides.                                         |
| <ApiBadge label="container" />  | <code>HTMLElement &#124; null</code>                                                      | document.body | Portal container element. Content is portaled to document.body by default. |
| <ApiBadge label="side" />       | <code><a href="#select-td-side">Side</a></code>                                           | 'bottom'      | Preferred side for positioning relative to trigger                         |
| <ApiBadge label="align" />      | <code><a href="#select-td-align">Align</a></code>                                         | 'start'       | Alignment relative to trigger                                              |
| <ApiBadge label="className" />  | <code>string</code>                                                                       | -             | Appends custom classes to the root slot of this part.                      |

#### Events {#select-content-events}

| Name                                      | Type                                           | Default | Description            |
| ----------------------------------------- | ---------------------------------------------- | ------- | ---------------------- |
| <ApiBadge label="onCloseAutoFocus" />     | <code>(event: FocusEvent) =&gt; void</code>    | -       | Focus handler on close |
| <ApiBadge label="onEscapeKeyDown" />      | <code>(event: KeyboardEvent) =&gt; void</code> | -       | Escape key handler     |
| <ApiBadge label="onPointerDownOutside" /> | <code>(event: PointerEvent) =&gt; void</code>  | -       | Outside click handler  |

#### Data attributes {#select-content-data-attributes}

| Attribute                                       | Applied when | Purpose                                                                     |
| ----------------------------------------------- | ------------ | --------------------------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot.                       |
| <ApiBadge label="data-size" />                  | Always       | Reflects the resolved `size` prop so theme recipes can scope size variants. |

### Select.Item {#select-item}

See
[Spar Select docs](https://spar.app.turkishtechlab.com/docs/Components/Select)
for primitive behavior.

#### Props {#select-item-props}

| Name                            | Type                                                                                      | Default | Description                                                                                                                                                                                                                                                                         |
| ------------------------------- | ----------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />   | <code>React.ReactNode</code>                                                              | -       | Visible item content. Set `label` alongside it to register the text used for typeahead and trigger display.                                                                                                                                                                         |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                                  | -       | Per-slot class name overrides.                                                                                                                                                                                                                                                      |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -       | Per-slot HTML attribute overrides.                                                                                                                                                                                                                                                  |
| <ApiBadge label="label" />      | <code>string</code>                                                                       | -       | Text label for this item. Shown in the trigger when this item is selected, and used as the search key for keyboard typeahead. Should be set whenever `children` is not plain text (e.g. contains icons or other elements) so the trigger can display a clean string representation. |
| <ApiBadge label="value" />      | <code>string</code>                                                                       | -       | Option value                                                                                                                                                                                                                                                                        |
| <ApiBadge label="disabled" />   | <code>boolean</code>                                                                      | false   | Disables the option                                                                                                                                                                                                                                                                 |
| <ApiBadge label="className" />  | <code>string</code>                                                                       | -       | Appends custom classes to the root slot of this part.                                                                                                                                                                                                                               |

#### Data attributes {#select-item-data-attributes}

| Attribute                                           | Applied when                                      | Purpose                                               |
| --------------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" />     | Always                                            | Stable selector for wrapper styling on the root slot. |
| <ApiBadge label="data-highlighted" />               | When the item is focused via keyboard or pointer. | Theme hook for the highlighted state.                 |
| <ApiBadge label="data-state=&quot;checked&quot;" /> | When the item matches the current value.          | Theme hook for the checked state.                     |
| <ApiBadge label="data-disabled" />                  | When the item `disabled` is true.                 | Theme hook for the disabled item state.               |

### Select.Group {#select-group}

See
[Spar Select docs](https://spar.app.turkishtechlab.com/docs/Components/Select)
for primitive behavior.

#### Data attributes {#select-group-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |

### Select.Label {#select-label}

See
[Spar Select docs](https://spar.app.turkishtechlab.com/docs/Components/Select)
for primitive behavior.

#### Data attributes {#select-label-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |

### Select.Separator {#select-separator}

See
[Spar Select docs](https://spar.app.turkishtechlab.com/docs/Components/Select)
for primitive behavior.

#### Data attributes {#select-separator-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |

### Type Definitions {#select-type-definitions}

| Name                                                                                                   | Definition                                                          |
| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- |
| <span id="select-td-selectsize"><ApiBadge label="SelectSize" /></span>                                 | <code>'small' &#124; 'base' &#124; 'large'</code>                   |
| <span id="select-td-selectcontentwidth"><ApiBadge label="SelectContentWidth" /></span>                 | <code>'trigger' &#124; 'content' &#124; number &#124; string</code> |
| <span id="select-td-selectindicatorrenderstate"><ApiBadge label="SelectIndicatorRenderState" /></span> | <code>&#123; isOpen: boolean &#125;</code>                          |
| <span id="select-td-selecttriggerslot"><ApiBadge label="SelectTriggerSlot" /></span>                   | <code>'root' &#124; 'value' &#124; 'indicator'</code>               |
| <span id="select-td-side"><ApiBadge label="Side" /></span>                                             | <code>'top' &#124; 'right' &#124; 'bottom' &#124; 'left'</code>     |
| <span id="select-td-align"><ApiBadge label="Align" /></span>                                           | <code>'start' &#124; 'center' &#124; 'end'</code>                   |

{/* api-tables:end */}
