---
title: Skeleton
description:
  A decorative loading placeholder that reserves layout while content is being
  fetched. Supports rectangular and circular shapes with optional shimmer.
sidebar_label: Skeleton
sidebar_position: 16
slug: /components/skeleton
---

import LiveCode from '@site/src/components/LiveCode';
import {
  Skeleton,
  ReactSparDemoRoot,
} from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const skeletonScope = { Skeleton };

{/* prettier-ignore-start */}

export const playgroundDemo = String.raw`
function PlaygroundDemo() {
  return (
    <div className="mx-auto flex w-full max-w-120 flex-col gap-4">
      <div className="flex items-center gap-3">
        <Skeleton shape="circle" height={48} />
        <div className="flex flex-1 flex-col gap-2">
          <Skeleton height={16} width="45%" />
          <Skeleton height={14} width="70%" />
        </div>
      </div>
      <Skeleton height={120} />
      <div className="flex flex-col gap-2">
        <Skeleton height={14} />
        <Skeleton height={14} width="88%" />
        <Skeleton height={14} width="62%" />
      </div>
    </div>
  );
}

render(<PlaygroundDemo />);
`;

export const shapesDemo = String.raw`
function ShapesDemo() {
  return (
    <div className="flex flex-wrap items-center justify-center gap-8">
      <div className="flex w-64 flex-col gap-3">
        <Skeleton height={20} />
        <Skeleton height={20} width="80%" />
        <Skeleton height={20} width="55%" />
      </div>
      <Skeleton shape="circle" height={72} />
    </div>
  );
}

render(<ShapesDemo />);
`;

export const sizingDemo = String.raw`
function SizingDemo() {
  return (
    <div className="mx-auto flex w-full max-w-120 flex-col gap-3">
      <Skeleton height={12} width="35%" />
      <Skeleton height={16} width="60%" />
      <Skeleton height={24} width={320} />
      <Skeleton height="4rem" width="100%" />
    </div>
  );
}

render(<SizingDemo />);
`;

export const animationDemo = String.raw`
function AnimationDemo() {
  return (
    <div className="mx-auto grid w-full max-w-120 gap-5 sm:grid-cols-2">
      <div className="flex flex-col gap-3">
        <Skeleton height={18} width="45%" />
        <Skeleton height={80} />
        <Skeleton height={14} width="70%" />
      </div>
      <div className="flex flex-col gap-3">
        <Skeleton animation="none" height={18} width="45%" />
        <Skeleton animation="none" height={80} />
        <Skeleton animation="none" height={14} width="70%" />
      </div>
    </div>
  );
}

render(<AnimationDemo />);
`;

{/* prettier-ignore-end */}

# Skeleton

`Skeleton` renders a visual placeholder for content that is still loading. Use
it when the final layout is known enough to reserve space and avoid a jumpy
transition when data arrives.

The component is decorative by default and renders `aria-hidden="true"`. Pair it
with surrounding loading text, a region state, or another status component when
the loading state needs to be announced.

## Usage

```tsx
import { Skeleton } from '@takeoff-ui/react-spar';
```

```tsx
<Skeleton height={16} width="60%" />
<Skeleton shape="circle" height={40} />
```

## Playground

<LiveCode
  code={playgroundDemo}
  noInline
  previewMinHeight={280}
  previewWrapper={ReactSparDemoRoot}
  scope={skeletonScope}
/>

## Shapes

`shape="rectangle"` renders a rounded bar. `shape="circle"` turns the root into
a full-radius disc whose diameter follows `height`.

<LiveCode
  code={shapesDemo}
  noInline
  previewMinHeight={160}
  previewWrapper={ReactSparDemoRoot}
  scope={skeletonScope}
/>

## Sizing

`width` and `height` accept numbers in pixels or any CSS length string. Omitting
`width` lets the placeholder fill its container.

<LiveCode
  code={sizingDemo}
  noInline
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={skeletonScope}
/>

## Animation

Use `animation="none"` for static placeholders, especially inside surfaces where
motion would distract from other loading or status indicators.

<LiveCode
  code={animationDemo}
  noInline
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={skeletonScope}
/>

## Accessibility

- `Skeleton` is hidden from assistive technology by default because it does not
  carry meaningful content on its own.
- Announce loading through nearby copy, a labeled region with `aria-busy`, or a
  [`Spinner`](/docs/components/spinner) when users need active status feedback.
- Avoid replacing labels, headings, or controls with focusable placeholders.
  Render the real interactive element only when it is ready.

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Skeleton {#skeleton}

#### Props {#skeleton-props}

| Name                             | Type                                                                                                                                    | Default     | Description                                                                                                                       |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="shape" />       | <code><a href="#skeleton-td-skeletonshape">SkeletonShape</a></code>                                                                     | 'rectangle' | Placeholder silhouette. `rectangle` renders the rounded bar, `circle` a full-radius disc whose diameter tracks `height`.          |
| <ApiBadge label="animation" />   | <code><a href="#skeleton-td-skeletonanimation">SkeletonAnimation</a></code>                                                             | 'shimmer'   | Loading animation. `shimmer` sweeps the highlight strip across the bar; `none` renders a static placeholder.                      |
| <ApiBadge label="width" />       | <code>string &#124; number</code>                                                                                                       | -           | Fixed width, number in px or any CSS length. Published to the recipe as `--tk-skeleton-width`; defaults to filling the container. |
| <ApiBadge label="height" />      | <code>string &#124; number</code>                                                                                                       | -           | Fixed height, number in px or any CSS length. Published to the recipe as `--tk-skeleton-height`.                                  |
| <ApiBadge label="classNames" />  | <code>Partial&lt;Record&lt;<a href="#skeleton-td-skeletonslot">SkeletonSlot</a>, string&gt;&gt;</code>                                  | -           | Per-slot extra classes.                                                                                                           |
| <ApiBadge label="slotProps" />   | <code>Partial&lt;Record&lt;<a href="#skeleton-td-skeletonslot">SkeletonSlot</a>, React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -           | Per-slot HTML-attribute overrides.                                                                                                |
| <ApiBadge label="className" />   | <code>string</code>                                                                                                                     | -           | Appends custom classes to the root slot.                                                                                          |
| <ApiBadge label="aria-hidden" /> | <code>boolean &#124; "true" &#124; "false"</code>                                                                                       | true        | Keeps the placeholder out of the accessibility tree. Leave it hidden and expose loading context through nearby content.           |

#### Data attributes {#skeleton-data-attributes}

| Attribute                                          | Applied when             | Purpose                                                          |
| -------------------------------------------------- | ------------------------ | ---------------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" />    | Always                   | Stable selector for wrapper styling on the root slot.            |
| <ApiBadge label="data-type" />                     | Always                   | Reflects the resolved `shape` prop for theme recipe scoping.     |
| <ApiBadge label="data-animation" />                | Always                   | Reflects the resolved `animation` prop for theme recipe scoping. |
| <ApiBadge label="data-slot=&quot;shimmer&quot;" /> | Always on the inner span | Stable selector for the decorative shimmer strip.                |

### Type Definitions {#skeleton-type-definitions}

| Name                                                                                   | Definition                               |
| -------------------------------------------------------------------------------------- | ---------------------------------------- |
| <span id="skeleton-td-skeletonshape"><ApiBadge label="SkeletonShape" /></span>         | <code>'rectangle' &#124; 'circle'</code> |
| <span id="skeleton-td-skeletonanimation"><ApiBadge label="SkeletonAnimation" /></span> | <code>'shimmer' &#124; 'none'</code>     |
| <span id="skeleton-td-skeletonslot"><ApiBadge label="SkeletonSlot" /></span>           | <code>'root' &#124; 'shimmer'</code>     |

{/* api-tables:end */}
