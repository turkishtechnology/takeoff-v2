---
title: Divider
description:
  A separator between content sections. Supports horizontal and vertical
  orientation, line styles, and an optional aligned label.
sidebar_label: Divider
sidebar_position: 5
slug: /components/divider
---

import LiveCode from '@site/src/components/LiveCode';
import { Divider, ReactSparDemoRoot } from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const dividerScope = { Divider };

{/* prettier-ignore-start */}

export const playgroundDemo = String.raw`
function PlaygroundDemo() {
  return (
    <div className="w-full max-w-md">
      <p className="text-sm">Sign in with your membership number.</p>
      <Divider>OR</Divider>
      <p className="text-sm">Continue as a guest.</p>
    </div>
  );
}

render(<PlaygroundDemo />);
`;

export const orientationDemo = String.raw`
function OrientationDemo() {
  return (
    <div className="flex h-12 w-full max-w-md items-center justify-center text-sm">
      <span>Economy</span>
      <Divider orientation="vertical" />
      <span>Business</span>
      <Divider orientation="vertical" />
      <span>First</span>
    </div>
  );
}

render(<OrientationDemo />);
`;

export const appearancesDemo = String.raw`
function AppearancesDemo() {
  return (
    <div className="w-full max-w-md text-sm">
      <p>Solid</p>
      <Divider appearance="solid" />
      <p>Dashed</p>
      <Divider appearance="dashed" />
      <p>Dotted</p>
      <Divider appearance="dotted" />
    </div>
  );
}

render(<AppearancesDemo />);
`;

export const customizationDemo = String.raw`
function CustomizationDemo() {
  return (
    <div className="w-full max-w-md text-sm">
      <p>Line color</p>
      <Divider style={{ color: '#fca5a5' }}>Cancelled</Divider>
      <Divider style={{ color: '#38bdf8' }} appearance="dashed" />

      <p>Spacing</p>
      <Divider style={{ marginBlock: 4 }} />
      <Divider style={{ marginBlock: 4 }} />
      <Divider style={{ marginBlock: 32 }}>Wide spacing</Divider>

      <p>Label</p>
      <Divider slotProps={{ label: { style: { color: '#0369a1', fontWeight: 600, fontSize: 13 } } }}>
        Styled label
      </Divider>
      <Divider classNames={{ label: 'uppercase tracking-widest' }}>utility classes</Divider>

      <p>Label spacing</p>
      <Divider slotProps={{ label: { style: { paddingInline: 32 } } }}>Wide gap</Divider>
      <Divider slotProps={{ label: { style: { paddingInline: 0 } } }}>No gap</Divider>
    </div>
  );
}

render(<CustomizationDemo />);
`;

export const alignDemo = String.raw`
function AlignDemo() {
  return (
    <div className="w-full max-w-md">
      <Divider align="start">Section</Divider>
      <Divider>Section</Divider>
      <Divider align="end">Section</Divider>
    </div>
  );
}

render(<AlignDemo />);
`;

{/* prettier-ignore-end */}

# Divider

`Divider` separates sections of content or groups of items. It is a standalone
component (no upstream Spar primitive) that renders a semantic
`role="separator"` by default and draws its line entirely in CSS, so optional
label content can sit anywhere along the line.

## Usage

```tsx
import { Divider } from '@takeoff-ui/react-spar';
```

```tsx
<Divider>OR</Divider>
```

## Playground

<LiveCode
  code={playgroundDemo}
  noInline
  previewMinHeight={120}
  previewWrapper={ReactSparDemoRoot}
  scope={dividerScope}
/>

## Orientation

Vertical dividers stretch to the height of their flex container.

<LiveCode
  code={orientationDemo}
  noInline
  previewMinHeight={80}
  previewWrapper={ReactSparDemoRoot}
  scope={dividerScope}
/>

## Appearances

<LiveCode
  code={appearancesDemo}
  noInline
  previewMinHeight={160}
  previewWrapper={ReactSparDemoRoot}
  scope={dividerScope}
/>

## Customization

- **Line color** — the line follows `currentColor`; set `color` on the root via
  `style` or your own CSS. Dashed and dotted lines pick the color up too.
- **Spacing** — outer margins default to `16px` (`margin-block` for horizontal,
  `margin-inline` for vertical). Override them with inline `style` (e.g.
  `marginBlock`), which always wins over the recipe.
- **Label** — target the label slot with `classNames={{ label: … }}` for extra
  classes or `slotProps={{ label: { style: … } }}` for inline styles. The gap
  between the line and the label is the label's `paddingInline` (`paddingBlock`
  on vertical dividers), `8px` by default.

<LiveCode
  code={customizationDemo}
  noInline
  previewMinHeight={340}
  previewWrapper={ReactSparDemoRoot}
  scope={dividerScope}
/>

## Align

Use `align` to position the label along the line: `start`, `center` (the
default), or `end`.

<LiveCode
  code={alignDemo}
  noInline
  previewMinHeight={140}
  previewWrapper={ReactSparDemoRoot}
  scope={dividerScope}
/>

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Divider {#divider}

#### Props {#divider-props}

| Name                             | Type                                                                                                                                 | Default      | Description                                                                                                                                                                                              |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />    | <code>React.ReactNode</code>                                                                                                         | -            | Label content rendered between the line segments.                                                                                                                                                        |
| <ApiBadge label="orientation" /> | <code><a href="#divider-td-dividerorientation">DividerOrientation</a></code>                                                         | 'horizontal' | Axis of the divider line. Vertical dividers size from their container — place them in a flex row (or give the parent an explicit height) so the line has a height to fill.                               |
| <ApiBadge label="appearance" />  | <code><a href="#divider-td-dividerappearance">DividerAppearance</a></code>                                                           | 'solid'      | Line style.                                                                                                                                                                                              |
| <ApiBadge label="align" />       | <code><a href="#divider-td-divideralign">DividerAlign</a></code>                                                                     | 'center'     | Placement of the label along the divider line.                                                                                                                                                           |
| <ApiBadge label="decorative" />  | <code>boolean</code>                                                                                                                 | false        | Marks the divider as purely visual. Decorative dividers are removed from the accessibility tree (`role="none"`); non-decorative dividers render `role="separator"` with the matching `aria-orientation`. |
| <ApiBadge label="classNames" />  | <code>Partial&lt;Record&lt;<a href="#divider-td-dividerslot">DividerSlot</a>, string&gt;&gt;</code>                                  | -            | Per-slot extra classes.                                                                                                                                                                                  |
| <ApiBadge label="slotProps" />   | <code>Partial&lt;Record&lt;<a href="#divider-td-dividerslot">DividerSlot</a>, React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -            | Per-slot HTML-attribute overrides.                                                                                                                                                                       |
| <ApiBadge label="className" />   | <code>string</code>                                                                                                                  | -            | Appends custom classes to the root slot.                                                                                                                                                                 |

#### Data attributes {#divider-data-attributes}

| Attribute                                        | Applied when                 | Purpose                                                                     |
| ------------------------------------------------ | ---------------------------- | --------------------------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" />  | Always                       | Stable selector for wrapper styling on the root slot.                       |
| <ApiBadge label="data-slot=&quot;label&quot;" /> | When children are renderable | Stable selector for the wrapper-owned label slot.                           |
| <ApiBadge label="data-orientation" />            | Always                       | Reflects the resolved `orientation` prop for theme recipe scoping.          |
| <ApiBadge label="data-type" />                   | Always                       | Reflects the resolved `appearance` prop for theme recipe scoping.           |
| <ApiBadge label="data-align" />                  | Always                       | Reflects the resolved `align` prop that positions the label along the line. |

### Type Definitions {#divider-type-definitions}

| Name                                                                                    | Definition                                           |
| --------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| <span id="divider-td-dividerorientation"><ApiBadge label="DividerOrientation" /></span> | <code>'horizontal' &#124; 'vertical'</code>          |
| <span id="divider-td-dividerappearance"><ApiBadge label="DividerAppearance" /></span>   | <code>'solid' &#124; 'dashed' &#124; 'dotted'</code> |
| <span id="divider-td-divideralign"><ApiBadge label="DividerAlign" /></span>             | <code>'start' &#124; 'center' &#124; 'end'</code>    |
| <span id="divider-td-dividerslot"><ApiBadge label="DividerSlot" /></span>               | <code>'root' &#124; 'label'</code>                   |

{/* api-tables:end */}
