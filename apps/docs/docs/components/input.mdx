---
title: Input
description:
  Accessible text input with compound parts for label, container, helper text,
  error message, and adornments.
sidebar_label: Input
sidebar_position: 5
slug: /components/input
---

import LiveCode from '@site/src/components/LiveCode';
import {
  Field,
  Input,
  ReactSparDemoRoot,
} from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const inputScope = { Field, Input };

{/* prettier-ignore-start */}

export const inputDemoCss = String.raw`
.input-demo {
  display: flex;
  flex-direction: column;
  gap: 16px;
  width: min(100%, 360px);
}

.input-demo-row {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: flex-start;
}

.input-demo-row > * {
  flex: 1 1 200px;
}
`;

export const playgroundDemo = String.raw`
<Field>
  <Field.Label>Passenger name</Field.Label>
  <Input>
    <Input.Container>
      <Input.Field placeholder="Ada Lovelace" />
    </Input.Container>
  </Input>
  <Field.Description>
    Match the name on your travel document.
  </Field.Description>
</Field>
`;

export const fieldWithInputDemo = String.raw`
<div className="input-demo">
  <Field required>
    <Field.Label>Email</Field.Label>
    <Input>
      <Input.Container>
        <Input.Field type="email" placeholder="you@example.com" />
      </Input.Container>
    </Input>
    <Field.Description>We will send a booking confirmation.</Field.Description>
  </Field>

  <Field invalid>
    <Field.Label>Phone number</Field.Label>
    <Input>
      <Input.Container>
        <Input.Field defaultValue="not-a-phone" />
      </Input.Container>
    </Input>
    <Field.ErrorMessage>Enter a valid phone number.</Field.ErrorMessage>
  </Field>

  <Field disabled>
    <Field.Label>Disabled</Field.Label>
    <Input>
      <Input.Container>
        <Input.Field defaultValue="Booking locked" />
      </Input.Container>
    </Input>
  </Field>

  <Field readOnly>
    <Field.Label>Read-only</Field.Label>
    <Input>
      <Input.Container>
        <Input.Field defaultValue="TK-1928" />
      </Input.Container>
    </Input>
  </Field>
</div>
`;

export const sizesDemo = String.raw`
<div className="input-demo">
  <Field>
    <Field.Label>Small</Field.Label>
    <Input size="small">
      <Input.Container>
        <Input.Field placeholder="Compact" />
      </Input.Container>
    </Input>
  </Field>
  <Field>
    <Field.Label>Base</Field.Label>
    <Input size="base">
      <Input.Container>
        <Input.Field placeholder="Default" />
      </Input.Container>
    </Input>
  </Field>
  <Field>
    <Field.Label>Large</Field.Label>
    <Input size="large">
      <Input.Container>
        <Input.Field placeholder="Roomy" />
      </Input.Container>
    </Input>
  </Field>
</div>
`;

export const adornmentsDemo = String.raw`
<div className="input-demo">
  <Field>
    <Field.Label>Amount</Field.Label>
    <Input>
      <Input.Container>
        <Input.Prefix>USD</Input.Prefix>
        <Input.Field placeholder="0.00" inputMode="decimal" />
        <Input.Suffix>.00</Input.Suffix>
      </Input.Container>
    </Input>
  </Field>

  <Field>
    <Field.Label>Search flights</Field.Label>
    <Input>
      <Input.Container
        startContent={
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
            <circle cx="11" cy="11" r="7" />
            <line x1="21" y1="21" x2="16.65" y2="16.65" />
          </svg>
        }
      >
        <Input.Field placeholder="IST → LHR" />
      </Input.Container>
    </Input>
  </Field>
</div>
`;

{/* prettier-ignore-end */}

# Input

`Input` is a compound, accessible text input. The root owns visual state
(`size`, plus inherited `isInvalid` / `disabled` / `required` / `readOnly`),
`Input.Container` is the bordered row that hosts the field plus optional
adornments. Wrap `Input` in a `Field` to attach a label, helper text, or error
message — the field-level state cascades into the input automatically.

## Usage

```tsx
import { Field, Input } from '@takeoff-ui/react-spar';
```

```tsx
<Field>
  <Field.Label />
  <Input>
    <Input.Container startContent={<Icon />} endContent={<Icon />}>
      <Input.Prefix />
      <Input.Field />
      <Input.Suffix />
    </Input.Container>
  </Input>
  <Field.Description />
  <Field.ErrorMessage />
</Field>
```

## Playground

<LiveCode
  code={playgroundDemo}
  cssCode={inputDemoCss}
  previewMinHeight={160}
  previewWrapper={ReactSparDemoRoot}
  scope={inputScope}
/>

## Using with `Field`

`Field` is the generic ARIA wrapper. Setting `invalid`, `disabled`, `required`,
or `readOnly` on `Field` cascades into the nested `Input`, and `Field.Label`,
`Field.Description`, and `Field.ErrorMessage` are wired to the field control
through shared IDs.

<LiveCode
  code={fieldWithInputDemo}
  cssCode={inputDemoCss}
  editable={false}
  previewMinHeight={460}
  previewWrapper={ReactSparDemoRoot}
  scope={inputScope}
/>

## Sizes

<LiveCode
  code={sizesDemo}
  cssCode={inputDemoCss}
  editable={false}
  previewMinHeight={340}
  previewWrapper={ReactSparDemoRoot}
  scope={inputScope}
/>

## Prefix, Suffix & Icons

<LiveCode
  code={adornmentsDemo}
  cssCode={inputDemoCss}
  editable={false}
  previewMinHeight={240}
  previewWrapper={ReactSparDemoRoot}
  scope={inputScope}
/>

## Accessibility

- `Field.Label`, `Field.Description`, and `Field.ErrorMessage` are wired to the
  control via stable IDs derived from the `Field` root (`aria-labelledby`,
  `aria-describedby`, `aria-invalid`).
- The asterisk inside `Field.Label` is decorative — `required` is also surfaced
  to assistive tech via the input's native `required` / `aria-required`.
- `Input.Container`'s `startContent` and `endContent` slots render with
  `aria-hidden="true"`; put any meaningful affordance inside `Input.Suffix` /
  `Input.Prefix` (or as a focusable button child) instead.

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Input {#input}

See [Spar Input docs](https://spar.app.turkishtechlab.com/docs/Components/Input)
for primitive behavior.

#### Props {#input-props}

| Name                            | Type                                                                                | Default | Description                                                                                                                                                    |
| ------------------------------- | ----------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />   | <code>React.ReactNode</code>                                                        | -       | Compound parts (`Input.Container`, plus optional `Input.Prefix` / `Input.Field` / `Input.Suffix`). Wrap in a `Field` to attach labels and helper text.         |
| <ApiBadge label="size" />       | <code>InputSize</code>                                                              | 'base'  | Size scale.                                                                                                                                                    |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                            | -       | Per-slot class name overrides.                                                                                                                                 |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -       | Per-slot HTML attribute overrides.                                                                                                                             |
| <ApiBadge label="id" />         | <code>string</code>                                                                 | -       | Custom base ID for ARIA relationships. If not provided, one will be generated automatically. Sub-element IDs are derived as `${id}-field`, `${id}-label`, etc. |
| <ApiBadge label="disabled" />   | <code>boolean</code>                                                                | -       | Input disabled state. When inside a Field, inherited from Field.                                                                                               |
| <ApiBadge label="required" />   | <code>boolean</code>                                                                | -       | Input required state. When inside a Field, inherited from Field.                                                                                               |
| <ApiBadge label="readOnly" />   | <code>boolean</code>                                                                | -       | Input read-only state. When inside a Field, inherited from Field.                                                                                              |
| <ApiBadge label="isInvalid" />  | <code>boolean</code>                                                                | -       | Input validation state. When inside a Field, inherited from Field.                                                                                             |
| <ApiBadge label="className" />  | <code>string</code>                                                                 | -       | Appends custom classes to the root slot of this part.                                                                                                          |

#### Data attributes {#input-data-attributes}

| Attribute          | Applied when              | Purpose                                                                     |
| ------------------ | ------------------------- | --------------------------------------------------------------------------- |
| `data-slot="root"` | Always                    | Stable selector for wrapper styling on the root slot.                       |
| `data-size`        | Always                    | Reflects the resolved `size` prop so theme recipes can scope size variants. |
| `data-invalid`     | When `isInvalid` is true. | Theme hook for the invalid state. Emitted by Spar Input on the root.        |
| `data-disabled`    | When `disabled` is true.  | Theme hook for the disabled state. Emitted by Spar Input.                   |
| `data-required`    | When `required` is true.  | Theme hook used by the parent `Field` to auto-render its required asterisk. |
| `data-readonly`    | When `readOnly` is true.  | Theme hook for the read-only state. Emitted by Spar Input.                  |

### Input.Container {#input-container}

#### Props {#input-container-props}

| Name                              | Type                                                                                            | Default | Description                                                                                                                                                            |
| --------------------------------- | ----------------------------------------------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />     | <code>React.ReactNode</code>                                                                    | -       | `Input.Prefix`, `Input.Field`, `Input.Suffix`. Leading / trailing icons go through the `startContent` / `endContent` props.                                            |
| <ApiBadge label="startContent" /> | <code>React.ReactNode</code>                                                                    | -       | Content rendered before the field — typically an icon, but accepts any node (chip, button, etc.). Rendered inside the `startContent` slot (`.tk-input-start-content`). |
| <ApiBadge label="endContent" />   | <code>React.ReactNode</code>                                                                    | -       | Content rendered after the field. Same shape as `startContent`, rendered inside the `endContent` slot (`.tk-input-end-content`).                                       |
| <ApiBadge label="classNames" />   | <code>Partial&lt;Record&lt;InputContainerSlot, string&gt;&gt;</code>                            | -       | Per-slot class name overrides.                                                                                                                                         |
| <ApiBadge label="slotProps" />    | <code>Partial&lt;Record&lt;InputContainerSlot, HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -       | Per-slot HTML attribute overrides.                                                                                                                                     |
| <ApiBadge label="className" />    | <code>string</code>                                                                             | -       | Appends custom classes to the root slot of this part.                                                                                                                  |

#### Data attributes {#input-container-data-attributes}

| Attribute          | Applied when                       | Purpose                                                          |
| ------------------ | ---------------------------------- | ---------------------------------------------------------------- |
| `data-slot="root"` | Always                             | Stable selector for wrapper styling on the root slot.            |
| `data-invalid`     | When the root `isInvalid` is true. | Borders/background switch to the danger state on the visual row. |
| `data-disabled`    | When the root `disabled` is true.  | Theme hook for the disabled visual row.                          |
| `data-readonly`    | When the root `readOnly` is true.  | Theme hook for the read-only visual row.                         |

### Input.Field {#input-field}

See [Spar Input docs](https://spar.app.turkishtechlab.com/docs/Components/Input)
for primitive behavior.

#### Props {#input-field-props}

| Name                            | Type                                                                                | Default | Description                                           |
| ------------------------------- | ----------------------------------------------------------------------------------- | ------- | ----------------------------------------------------- |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                            | -       | Per-slot class name overrides.                        |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -       | Per-slot HTML attribute overrides.                    |
| <ApiBadge label="autoFocus" />  | <code>boolean</code>                                                                | false   | Whether to focus the input on mount                   |
| <ApiBadge label="className" />  | <code>string</code>                                                                 | -       | Appends custom classes to the root slot of this part. |

#### Data attributes {#input-field-data-attributes}

| Attribute          | Applied when | Purpose                                                                     |
| ------------------ | ------------ | --------------------------------------------------------------------------- |
| `data-slot="root"` | Always       | Stable selector for wrapper styling on the root slot.                       |
| `data-size`        | Always       | Reflects the resolved `size` prop so theme recipes can scope size variants. |

### Input.Prefix {#input-prefix}

#### Data attributes {#input-prefix-data-attributes}

| Attribute          | Applied when | Purpose                                               |
| ------------------ | ------------ | ----------------------------------------------------- |
| `data-slot="root"` | Always       | Stable selector for wrapper styling on the root slot. |

### Input.Suffix {#input-suffix}

#### Data attributes {#input-suffix-data-attributes}

| Attribute          | Applied when | Purpose                                               |
| ------------------ | ------------ | ----------------------------------------------------- |
| `data-slot="root"` | Always       | Stable selector for wrapper styling on the root slot. |

{/* api-tables:end */}
