---
title: Accordion
description: Compound Accordion demos for @takeoff-ui/react-spar.
sidebar_label: Accordion
sidebar_position: 1
slug: /components/accordion
---

import LiveCode from '@site/src/components/LiveCode';
import {
  Accordion,
  ReactSparDemoRoot,
} from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const accordionScope = { Accordion };

export const accordionDemoCss = `
.accordion-demo {
  width: min(100%, 720px);
}

.accordion-demo-stack { display: grid; width: min(100%, 720px); gap: 12px; }

.accordion-demo-note { width: fit-content; border: 1px solid
var(--border-light); border-radius: 999px; background: var(--static-light);
color: var(--text-dark); font-size: 13px; font-weight: 600; line-height: 1;
padding: 8px 12px; }

.accordion-demo-icon { display: inline-grid; width: 24px; height: 24px;
place-items: center; border-radius: 999px; background: var(--primary-base);
color: var(--static-white); }

.accordion-demo-icon svg { display: block; } `;

export const playgroundDemo = `
<Accordion className="accordion-demo" defaultActiveIndex="fare">
  <Accordion.Item itemKey="fare">
    <Accordion.Header>
      <Accordion.Trigger>Fare conditions</Accordion.Trigger>
    </Accordion.Header>
    <Accordion.Content>
      Review refund windows, change fees, and cabin rules before confirming the
      itinerary.
    </Accordion.Content>
  </Accordion.Item>

<Accordion.Item itemKey="baggage">
  <Accordion.Header>
    <Accordion.Trigger>Baggage allowance</Accordion.Trigger>
  </Accordion.Header>
  <Accordion.Content>
    Carry-on, checked baggage, and sports equipment allowances stay grouped in
    one collapsible panel.
  </Accordion.Content>
</Accordion.Item>

  <Accordion.Item itemKey="check-in">
    <Accordion.Header>
      <Accordion.Trigger>Online check-in</Accordion.Trigger>
    </Accordion.Header>
    <Accordion.Content>
      Use the trigger text as the accessible heading, then place rich content in
      the content part.
    </Accordion.Content>
  </Accordion.Item>
</Accordion>
`;

export const multipleDemo = `
<Accordion
  allowMultiple
  className="accordion-demo"
  defaultActiveIndex={['boarding', 'baggage']}
  mode="compact"
  type="divided"
>
  <Accordion.Item itemKey="boarding">
    <Accordion.Header>
      <Accordion.Trigger>Boarding pass</Accordion.Trigger>
    </Accordion.Header>
    <Accordion.Content>
      Show mobile pass availability, airport counter fallbacks, and printable
      document rules together.
    </Accordion.Content>
  </Accordion.Item>

<Accordion.Item itemKey="baggage">
  <Accordion.Header>
    <Accordion.Trigger>Bag drop</Accordion.Trigger>
  </Accordion.Header>
  <Accordion.Content>
    Multiple panels can remain open when the workflow asks users to compare
    related instructions.
  </Accordion.Content>
</Accordion.Item>

  <Accordion.Item itemKey="security">
    <Accordion.Header>
      <Accordion.Trigger>Security notes</Accordion.Trigger>
    </Accordion.Header>
    <Accordion.Content>
      Compact mode keeps dense operational guidance scannable without changing
      the compound anatomy.
    </Accordion.Content>
  </Accordion.Item>
</Accordion>
`;

export const controlledDemo = `
function ControlledAccordionDemo() {
  const [activeIndex, setActiveIndex] = useState('documents');

return ( <div className="accordion-demo-stack">

<div className="accordion-demo-note">Active panel: {activeIndex}</div>
<Accordion
        activeIndex={activeIndex}
        className="accordion-demo"
        onActiveIndexChange={setActiveIndex}
        preventCollapse
      > <Accordion.Item itemKey="documents"> <Accordion.Header>
<Accordion.Trigger>Travel documents</Accordion.Trigger> </Accordion.Header>
<Accordion.Content> Controlled state lets app logic decide which passenger task
remains expanded. </Accordion.Content> </Accordion.Item>

        <Accordion.Item itemKey="seat">
          <Accordion.Header>
            <Accordion.Trigger>Seat selection</Accordion.Trigger>
          </Accordion.Header>
          <Accordion.Content>
            Pair activeIndex with onActiveIndexChange when the open panel should
            sync with route state, analytics, or form progress.
          </Accordion.Content>
        </Accordion.Item>
      </Accordion>
    </div>

); }

render(<ControlledAccordionDemo />); `;

export const customIconsDemo = `
const ExpandIcon = () => (
  <span className="accordion-demo-icon" aria-hidden="true">
    <svg
      width="14"
      height="14"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2.5"
      strokeLinecap="round"
    >
      <line x1="12" y1="5" x2="12" y2="19" />
      <line x1="5" y1="12" x2="19" y2="12" />
    </svg>
  </span>
);

const CollapseIcon = () => (

<span className="accordion-demo-icon" aria-hidden="true">
  <svg
    width="14"
    height="14"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth="2.5"
    strokeLinecap="round"
  >
    <line x1="5" y1="12" x2="19" y2="12" />
  </svg>
</span>
);

render(

  <Accordion
    arrowPosition="left"
    className="accordion-demo accordion-demo-large"
    collapseIcon={<CollapseIcon />}
    defaultActiveIndex="service"
    expandIcon={<ExpandIcon />}
    size="large"
  >
    <Accordion.Item itemKey="service">
      <Accordion.Header level={2}>
        <Accordion.Trigger>Service options</Accordion.Trigger>
      </Accordion.Header>
      <Accordion.Content>
        Custom icons are decorative and stay inside Accordion.Trigger, so the
        public anatomy remains Item, Header, Trigger, and Content.
      </Accordion.Content>
    </Accordion.Item>

    <Accordion.Item itemKey="support">
      <Accordion.Header level={2}>
        <Accordion.Trigger>Support channels</Accordion.Trigger>
      </Accordion.Header>
      <Accordion.Content>
        Use arrowPosition when the surrounding layout expects the disclosure
        affordance before the label.
      </Accordion.Content>
    </Accordion.Item>

  </Accordion>,
);
`;

# Accordion

`Accordion` is a compound disclosure component. Compose `Accordion.Item`,
`Accordion.Header`, `Accordion.Trigger`, and `Accordion.Content`; keep behavior
props on the root so Spar owns the open/closed state model.

## Playground

<LiveCode
  code={playgroundDemo}
  cssCode={accordionDemoCss}
  previewMinHeight={360}
  previewWrapper={ReactSparDemoRoot}
  scope={accordionScope}
/>

## Multiple Panels

<LiveCode
  code={multipleDemo}
  cssCode={accordionDemoCss}
  editable={false}
  previewMinHeight={360}
  previewWrapper={ReactSparDemoRoot}
  scope={accordionScope}
/>

## Controlled

<LiveCode
  code={controlledDemo}
  cssCode={accordionDemoCss}
  editable={false}
  noInline
  previewMinHeight={320}
  previewWrapper={ReactSparDemoRoot}
  scope={accordionScope}
/>

## Custom Arrows

<LiveCode
  code={customIconsDemo}
  cssCode={accordionDemoCss}
  editable={false}
  noInline
  previewMinHeight={300}
  previewWrapper={ReactSparDemoRoot}
  scope={accordionScope}
/>

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Accordion {#accordion}

#### Props {#accordion-props}

| Name                                    | Type                                                                                | Default                          | Description                                                                                                        |
| --------------------------------------- | ----------------------------------------------------------------------------------- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| <ApiBadge label="children" />           | <code>React.ReactNode</code>                                                        | undefined                        | `Accordion.Item` elements rendered inside the disclosure container.                                                |
| <ApiBadge label="type" />               | <code>AccordionType</code>                                                          | 'grouped'                        | Visual grouping. `'compact'` is a deprecated alias for `(type='grouped', mode='compact')` and emits a dev warning. |
| <ApiBadge label="mode" />               | <code>AccordionMode</code>                                                          | 'default'                        | Density mode. Pairs with any `AccordionType`.                                                                      |
| <ApiBadge label="size" />               | <code>AccordionSize</code>                                                          | 'base'                           | Size scale.                                                                                                        |
| <ApiBadge label="arrowPosition" />      | <code>AccordionArrowPosition</code>                                                 | 'right'                          | Position of the auto-rendered arrow inside each trigger.                                                           |
| <ApiBadge label="hideArrows" />         | <code>boolean</code>                                                                | false                            | Hide the auto-rendered arrow on every trigger.                                                                     |
| <ApiBadge label="expandIcon" />         | <code>React.ReactNode</code>                                                        | a built-in chevron pointing down | Visual content for the arrow when an item is collapsed.                                                            |
| <ApiBadge label="collapseIcon" />       | <code>React.ReactNode</code>                                                        | a built-in chevron pointing up   | Visual content for the arrow when an item is expanded.                                                             |
| <ApiBadge label="allowMultiple" />      | <code>boolean</code>                                                                | undefined                        | When `true`, multiple items can be expanded simultaneously.                                                        |
| <ApiBadge label="activeIndex" />        | <code>AccordionActiveIndex</code>                                                   | undefined                        | Controlled active item identifier(s).                                                                              |
| <ApiBadge label="defaultActiveIndex" /> | <code>AccordionActiveIndex</code>                                                   | undefined                        | Uncontrolled initial active item identifier(s).                                                                    |
| <ApiBadge label="preventCollapse" />    | <code>boolean</code>                                                                | undefined                        | When `true`, single-mode items cannot be collapsed by clicking again.                                              |
| <ApiBadge label="disabled" />           | <code>boolean</code>                                                                | undefined                        | Disables every item in the accordion.                                                                              |
| <ApiBadge label="orientation" />        | <code>"vertical" &#124; "horizontal"</code>                                         | undefined                        | Orientation for keyboard navigation.                                                                               |
| <ApiBadge label="classNames" />         | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                            | undefined                        | Per-slot class name overrides.                                                                                     |
| <ApiBadge label="slotProps" />          | <code>Partial&lt;Record&lt;"root", HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | undefined                        | Per-slot HTML attribute overrides.                                                                                 |
| <ApiBadge label="className" />          | <code>string</code>                                                                 | undefined                        | Appends custom classes to the root slot of this part.                                                              |

#### Events {#accordion-events}

| Name                                     | Type                                                 | Default   | Description                        |
| ---------------------------------------- | ---------------------------------------------------- | --------- | ---------------------------------- |
| <ApiBadge label="onActiveIndexChange" /> | <code>(next: AccordionActiveIndex) =&gt; void</code> | undefined | Fired when the active set changes. |

#### Data attributes {#accordion-data-attributes}

| Attribute             | Applied when | Purpose                                                                                                |
| --------------------- | ------------ | ------------------------------------------------------------------------------------------------------ |
| `data-slot="root"`    | Always       | Stable selector for wrapper styling on the root slot.                                                  |
| `data-mode`           | Always       | Reflects the resolved `mode` prop. Theme recipes can scope per-mode rules without reading React props. |
| `data-size`           | Always       | Reflects the resolved `size` prop so theme recipes can scope size variants.                            |
| `data-arrow-position` | Always       | Reflects `arrowPosition`. Theme recipes can flip arrow alignment without reading the React prop.       |

### Accordion.Item {#accordion-item}

#### Props {#accordion-item-props}

| Name                            | Type                                                                                | Default   | Description                                                                                                                                                                     |
| ------------------------------- | ----------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />   | <code>React.ReactNode</code>                                                        | undefined | `Accordion.Header` and `Accordion.Content` elements that compose the item.                                                                                                      |
| <ApiBadge label="itemKey" />    | <code>AccordionItemKey</code>                                                       | undefined | Stable identity for this item. Required so root controlled props (`activeIndex`, `defaultActiveIndex`) can target this item reliably. Forwarded to the Spar primitive verbatim. |
| <ApiBadge label="disabled" />   | <code>boolean</code>                                                                | undefined | Disables this item only.                                                                                                                                                        |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                            | undefined | Per-slot class name overrides.                                                                                                                                                  |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | undefined | Per-slot HTML attribute overrides.                                                                                                                                              |
| <ApiBadge label="className" />  | <code>string</code>                                                                 | undefined | Appends custom classes to the root slot of this part.                                                                                                                           |

#### Data attributes {#accordion-item-data-attributes}

| Attribute          | Applied when           | Purpose                                                                                                                                                             |
| ------------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `data-slot="root"` | Always                 | Stable selector for wrapper styling on the root slot.                                                                                                               |
| `data-type`        | Always                 | Reflects the resolved `type` prop after legacy `compact` normalization. Theme recipes can target items per type.                                                    |
| `data-mode`        | Always                 | Reflects the resolved `mode` prop. Theme recipes can scope per-mode rules without reading React props.                                                              |
| `data-size`        | Always                 | Reflects the resolved `size` prop so theme recipes can scope size variants.                                                                                         |
| `data-open`        | When the item is open. | Stable open-state hook. Mirrors the active-index match independently of `data-state` so theme rules stay attached when `forceMount` keeps closed panels in the DOM. |

### Accordion.Header {#accordion-header}

#### Props {#accordion-header-props}

| Name                            | Type                                                                                | Default   | Description                                                  |
| ------------------------------- | ----------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------ |
| <ApiBadge label="children" />   | <code>React.ReactNode</code>                                                        | undefined | `Accordion.Trigger` element rendered inside the heading tag. |
| <ApiBadge label="level" />      | <code>number</code>                                                                 | undefined | HTML heading level (1-6).                                    |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                            | undefined | Per-slot class name overrides.                               |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | undefined | Per-slot HTML attribute overrides.                           |
| <ApiBadge label="className" />  | <code>string</code>                                                                 | undefined | Appends custom classes to the root slot of this part.        |

#### Data attributes {#accordion-header-data-attributes}

| Attribute          | Applied when | Purpose                                               |
| ------------------ | ------------ | ----------------------------------------------------- |
| `data-slot="root"` | Always       | Stable selector for wrapper styling on the root slot. |

### Accordion.Trigger {#accordion-trigger}

#### Props {#accordion-trigger-props}

| Name                            | Type                                                                                | Default   | Description                                           |
| ------------------------------- | ----------------------------------------------------------------------------------- | --------- | ----------------------------------------------------- |
| <ApiBadge label="children" />   | <code>React.ReactNode</code>                                                        | undefined | Accessible label for the trigger button.              |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                            | undefined | Per-slot class name overrides.                        |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | undefined | Per-slot HTML attribute overrides.                    |
| <ApiBadge label="className" />  | <code>string</code>                                                                 | undefined | Appends custom classes to the root slot of this part. |

#### Data attributes {#accordion-trigger-data-attributes}

| Attribute          | Applied when | Purpose                                               |
| ------------------ | ------------ | ----------------------------------------------------- |
| `data-slot="root"` | Always       | Stable selector for wrapper styling on the root slot. |

### Accordion.Content {#accordion-content}

#### Props {#accordion-content-props}

| Name                            | Type                                                                                | Default   | Description                                           |
| ------------------------------- | ----------------------------------------------------------------------------------- | --------- | ----------------------------------------------------- |
| <ApiBadge label="children" />   | <code>React.ReactNode</code>                                                        | undefined | Panel body. Animated open/closed by Spar.             |
| <ApiBadge label="forceMount" /> | <code>boolean</code>                                                                | undefined | Render content even while collapsed.                  |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                            | undefined | Per-slot class name overrides.                        |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | undefined | Per-slot HTML attribute overrides.                    |
| <ApiBadge label="className" />  | <code>string</code>                                                                 | undefined | Appends custom classes to the root slot of this part. |

#### Data attributes {#accordion-content-data-attributes}

| Attribute          | Applied when            | Purpose                                                                                                                                                                                              |
| ------------------ | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `data-slot="root"` | Always                  | Stable selector for wrapper styling on the root slot.                                                                                                                                                |
| `data-open`        | When the panel is open. | Stable open-state hook for the panel. Mirrors `useCollapsibleContext().isOpen` so theme animations and `max-height` rules stay in sync even when `forceMount` keeps the panel rendered while closed. |

{/* api-tables:end */}
