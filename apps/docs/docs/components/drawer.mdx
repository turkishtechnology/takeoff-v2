---
title: Drawer
description:
  A slide-in side panel built on top of Spar's Dialog primitive. Use it for
  navigation menus, detail views, filters, or any content that should overlay
  the current page without a full-page transition.
sidebar_label: Drawer
sidebar_position: 3
slug: /components/drawer
---

import LiveCode from '@site/src/components/LiveCode';
import {
  Badge,
  Button,
  Drawer,
  ReactSparDemoRoot,
} from '@site/src/components/ReactSparDocs';
import { ApiBadge } from '@site/src/components/ApiBadge';

export const drawerScope = { Badge, Button, Drawer };

{/* prettier-ignore-start */}

export const playgroundDemo = String.raw`
function PlaygroundDemo() {
  return (
    <Drawer placement="right">
      <Drawer.Trigger as={Button}>Open Drawer</Drawer.Trigger>
      <Drawer.Overlay />
      <Drawer.Panel>
        <Drawer.Header>
          <Drawer.Title>Flight Details</Drawer.Title>
          <Drawer.Close>✕</Drawer.Close>
        </Drawer.Header>
        <Drawer.Body>
          <Drawer.Description>
            Review your selected flight information and passenger details before confirming the booking.
          </Drawer.Description>
        </Drawer.Body>
      </Drawer.Panel>
    </Drawer>
  );
}

render(<PlaygroundDemo />);
`;

export const placementDemo = String.raw`
function PlacementDemo() {
  const [placement, setPlacement] = useState('right');

  return (
    <div className="flex flex-wrap justify-center gap-3">
      {['left', 'right', 'top', 'bottom', 'full-screen'].map((p) => (
        <Drawer key={p} placement={p}>
          <Drawer.Trigger as={Button}>{p}</Drawer.Trigger>
          <Drawer.Overlay />
          <Drawer.Panel>
            <Drawer.Header>
              <Drawer.Title>Drawer from {p}</Drawer.Title>
              <Drawer.Close>✕</Drawer.Close>
            </Drawer.Header>
            <Drawer.Body>
              This drawer slides in from the <strong>{p}</strong> side.
            </Drawer.Body>
          </Drawer.Panel>
        </Drawer>
      ))}
    </div>
  );
}

render(<PlacementDemo />);
`;


export const footerTypeDemo = String.raw`
function FooterTypeDrawerDemo() {
  const footerTypes = ['basic', 'divided', 'light'];

  return (
    <div className="flex flex-wrap justify-center gap-3">
      {footerTypes.map((type) => (
        <Drawer key={type} placement="right">
          <Drawer.Trigger as={Button}>{type}</Drawer.Trigger>
          <Drawer.Overlay />
          <Drawer.Panel>
            <Drawer.Header>
              <Drawer.Title>{type} footer</Drawer.Title>
              <Drawer.Close>✕</Drawer.Close>
            </Drawer.Header>
            <Drawer.Body>
              This footer uses <code>{type}</code> styling.
            </Drawer.Body>
            <Drawer.Footer footerType={type}>
              <Button variant="secondary" appearance="outlined">Cancel</Button>
              <Button>Confirm</Button>
            </Drawer.Footer>
          </Drawer.Panel>
        </Drawer>
      ))}
    </div>
  );
}

render(<FooterTypeDrawerDemo />);
`;

export const headerTypeDemo = String.raw`
function HeaderTypeDrawerDemo() {
  const headerTypes = ['basic', 'divided', 'light', 'dark', 'primary'];

  return (
    <div className="flex flex-wrap justify-center gap-3">
      {headerTypes.map((type) => (
        <Drawer key={type} placement="right">
          <Drawer.Trigger as={Button}>{type}</Drawer.Trigger>
          <Drawer.Overlay />
          <Drawer.Panel>
            <Drawer.Header headerType={type}>
              <Drawer.Title>{type} header</Drawer.Title>
              <Drawer.Close>✕</Drawer.Close>
            </Drawer.Header>
            <Drawer.Body>
              Header type: {type}
            </Drawer.Body>
          </Drawer.Panel>
        </Drawer>
      ))}
    </div>
  );
}

render(<HeaderTypeDrawerDemo />);
`;

export const dismissibleDemo = String.raw`
function DismissibleDrawerDemo() {
  const [open, setOpen] = useState(false);

  return (
    <Drawer open={open} onOpenChange={setOpen} dismissible={false} placement="right">
      <Drawer.Trigger as={Button}>Open non-dismissible</Drawer.Trigger>
      <Drawer.Overlay />
      <Drawer.Panel>
        <Drawer.Header>
          <Drawer.Title>Sticky Drawer</Drawer.Title>
          <Drawer.Close>✕</Drawer.Close>
        </Drawer.Header>
        <Drawer.Body>
          This drawer can only be closed using the close control.
        </Drawer.Body>
      </Drawer.Panel>
    </Drawer>
  );
}

render(<DismissibleDrawerDemo />);
`;


export const overlayBehaviorDemo = String.raw`
function OverlayBehaviorDemo() {
  return (
    <div className="flex flex-wrap justify-center gap-3">
      <Drawer placement="right">
        <Drawer.Trigger as={Button}>Blur</Drawer.Trigger>
        <Drawer.Overlay blur />
        <Drawer.Panel>
          <Drawer.Header>
            <Drawer.Title>Blurred overlay</Drawer.Title>
            <Drawer.Close>✕</Drawer.Close>
          </Drawer.Header>
          <Drawer.Body>
            The backdrop stays visible and adds a soft blur effect behind the panel.
          </Drawer.Body>
        </Drawer.Panel>
      </Drawer>

      <Drawer placement="right">
        <Drawer.Trigger as={Button}>Invisible</Drawer.Trigger>
        <Drawer.Overlay invisible />
        <Drawer.Panel>
          <Drawer.Header>
            <Drawer.Title>Invisible overlay</Drawer.Title>
            <Drawer.Close>✕</Drawer.Close>
          </Drawer.Header>
          <Drawer.Body>
            The overlay remains mounted for interaction but is visually hidden.
          </Drawer.Body>
        </Drawer.Panel>
      </Drawer>
    </div>
  );
}

render(<OverlayBehaviorDemo />);
`;

export const intensityDemo = String.raw`
function OverlayIntensityDemo() {
  const intensities = ['lightest', 'light', 'base', 'dark', 'darkest'];

  return (
    <div className="flex flex-wrap justify-center gap-3">
      {intensities.map((intensity) => (
        <Drawer key={intensity} placement="right">
          <Drawer.Trigger as={Button}>{intensity}</Drawer.Trigger>
          <Drawer.Overlay intensity={intensity} />
          <Drawer.Panel>
            <Drawer.Header>
              <Drawer.Title>{intensity}</Drawer.Title>
              <Drawer.Close>✕</Drawer.Close>
            </Drawer.Header>
            <Drawer.Body>Overlay intensity: {intensity}</Drawer.Body>
          </Drawer.Panel>
        </Drawer>
      ))}
    </div>
  );
}

render(<OverlayIntensityDemo />);
`;

{/* prettier-ignore-end */}

# Drawer

`Drawer` is a slide-in side panel that wraps Spar's Dialog primitive in modal
mode. It provides a compound component API for building navigation menus, detail
views, filters, or contextual forms that overlay the current page.

## Usage

```tsx
import { Drawer } from '@takeoff-ui/react-spar';
```

```tsx
<Drawer>
  <Drawer.Trigger />
  <Drawer.Overlay />
  <Drawer.Panel>
    <Drawer.Header>
      <Drawer.Title />
      <Drawer.Close>✕</Drawer.Close>
    </Drawer.Header>
    <Drawer.Body>
      <Drawer.Description />
    </Drawer.Body>
    <Drawer.Footer />
  </Drawer.Panel>
</Drawer>
```

## Playground

<LiveCode
  code={playgroundDemo}
  noInline
  previewMinHeight={200}
  previewWrapper={ReactSparDemoRoot}
  scope={drawerScope}
/>

## Placement

The `placement` prop controls from which side the drawer slides in.

<LiveCode
  code={placementDemo}
  noInline
  previewMinHeight={260}
  previewWrapper={ReactSparDemoRoot}
  scope={drawerScope}
/>

## Footer Types

`Drawer.Footer` supports a few visual variants through the `footerType` prop.
Use `divided` to add a separation line or `light` to soften the footer
background.

<LiveCode
  code={footerTypeDemo}
  noInline
  previewMinHeight={260}
  previewWrapper={ReactSparDemoRoot}
  scope={drawerScope}
/>

## Header Types

`Drawer.Header` supports multiple visual variants via the `headerType` prop. Use
this option to add a divider, light or dark background, or a primary-style
header when the drawer content requires extra emphasis.

<LiveCode
  code={headerTypeDemo}
  noInline
  previewMinHeight={260}
  previewWrapper={ReactSparDemoRoot}
  scope={drawerScope}
/>

## Dismissible

Set `dismissible={false}` to prevent the drawer from closing when the user
clicks outside or presses Escape.

<LiveCode
  code={dismissibleDemo}
  noInline
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={drawerScope}
/>

## Overlay Behavior

Use `blur` for a softened backdrop and `invisible` when the overlay should stay
interactive but visually disappear.

<LiveCode
  code={overlayBehaviorDemo}
  noInline
  previewMinHeight={180}
  previewWrapper={ReactSparDemoRoot}
  scope={drawerScope}
/>

## Intensity

The `intensity` prop controls how dark the overlay appears.

<LiveCode
  code={intensityDemo}
  noInline
  previewMinHeight={220}
  previewWrapper={ReactSparDemoRoot}
  scope={drawerScope}
/>

## Accessibility & Keyboard

- The drawer renders as a modal `role="dialog"` with proper `aria-labelledby`
  and `aria-describedby` relationships.
- Focus is trapped inside the panel while open and restored on close.
- `Drawer.Title` provides the accessible label; `Drawer.Description` provides
  the accessible description.
- When `dismissible={false}`, pressing Escape and clicking outside will not
  close the drawer.

| Key                                   | Behavior                                          |
| ------------------------------------- | ------------------------------------------------- |
| <kbd>Escape</kbd>                     | Closes the drawer (unless `dismissible={false}`). |
| <kbd>Tab</kbd> / <kbd>Shift+Tab</kbd> | Cycles focus within the panel.                    |

## Animation

The drawer animates automatically via CSS transitions. The panel slides in from
the configured `placement` direction and the overlay fades in. No JavaScript
animation library is required.

- **Panel:** `transform` 0.3s with `cubic-bezier(0.4, 0, 0.2, 1)`
- **Overlay:** `opacity` 0.3s with `ease-in-out`
- **Full-screen:** Scale + opacity transition instead of slide.

## API Reference

{/* api-tables:start (generated by apps/docs/scripts/generate-api-mdx.mjs - do not edit by hand) */}

### Drawer {#drawer}

#### Props {#drawer-props}

| Name                             | Type                                                                  | Default | Description                                                                                                                                                                      |
| -------------------------------- | --------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="children" />    | <code>React.ReactNode</code>                                          | -       | Drawer parts rendered inside the root.                                                                                                                                           |
| <ApiBadge label="placement" />   | <code><a href="#drawer-td-drawerplacement">DrawerPlacement</a></code> | -       | Side the drawer slides in from.                                                                                                                                                  |
| <ApiBadge label="dismissible" /> | <code>boolean</code>                                                  | true    | Whether the drawer can be dismissed by clicking outside or pressing Escape.                                                                                                      |
| <ApiBadge label="id" />          | <code>string</code>                                                   | -       | Custom base ID for ARIA relationships. If not provided, one will be generated automatically. Sub-element IDs are derived as `${id}-title`, `${id}-description`, `${id}-content`. |
| <ApiBadge label="disabled" />    | <code>boolean</code>                                                  | false   | Disables all dialog triggers (prevents opening)                                                                                                                                  |
| <ApiBadge label="open" />        | <code>boolean</code>                                                  | -       | Controlled open state                                                                                                                                                            |
| <ApiBadge label="defaultOpen" /> | <code>boolean</code>                                                  | false   | Initial open state (uncontrolled)                                                                                                                                                |

#### Events {#drawer-events}

| Name                              | Type                                    | Default | Description                      |
| --------------------------------- | --------------------------------------- | ------- | -------------------------------- |
| <ApiBadge label="onOpenChange" /> | <code>(open: boolean) =&gt; void</code> | -       | Callback when open state changes |

### Drawer.Trigger {#drawer-trigger}

#### Data attributes {#drawer-trigger-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |

### Drawer.Overlay {#drawer-overlay}

#### Props {#drawer-overlay-props}

| Name                            | Type                                                                                      | Default       | Description                                                                |
| ------------------------------- | ----------------------------------------------------------------------------------------- | ------------- | -------------------------------------------------------------------------- |
| <ApiBadge label="invisible" />  | <code>boolean</code>                                                                      | false         | When true, the overlay is rendered but visually invisible.                 |
| <ApiBadge label="intensity" />  | <code><a href="#drawer-td-draweroverlayintensity">DrawerOverlayIntensity</a></code>       | 'base'        | Overlay backdrop intensity.                                                |
| <ApiBadge label="blur" />       | <code>boolean</code>                                                                      | false         | Applies backdrop blur on the overlay.                                      |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                                  | -             |                                                                            |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -             |                                                                            |
| <ApiBadge label="container" />  | <code>HTMLElement &#124; null</code>                                                      | document.body | Portal container element. Content is portaled to document.body by default. |

#### Data attributes {#drawer-overlay-data-attributes}

| Attribute                                       | Applied when             | Purpose                                                     |
| ----------------------------------------------- | ------------------------ | ----------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always                   | Stable selector for wrapper styling on the root slot.       |
| <ApiBadge label="data-state" />                 | Always                   | `"open"` or `"closed"` — used for entry/exit animations.    |
| <ApiBadge label="data-intensity" />             | Always                   | Reflects the resolved `intensity` prop.                     |
| <ApiBadge label="data-invisible" />             | When `invisible` is true | Indicates overlay is visually hidden but still interactive. |
| <ApiBadge label="data-blur" />                  | When `blur` is true      | Enables backdrop blur styling on the overlay.               |

### Drawer.Panel {#drawer-panel}

#### Props {#drawer-panel-props}

| Name                              | Type                                                                                      | Default       | Description                                                                |
| --------------------------------- | ----------------------------------------------------------------------------------------- | ------------- | -------------------------------------------------------------------------- |
| <ApiBadge label="classNames" />   | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                                  | -             |                                                                            |
| <ApiBadge label="slotProps" />    | <code>Partial&lt;Record&lt;"root", React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -             |                                                                            |
| <ApiBadge label="container" />    | <code>HTMLElement &#124; null</code>                                                      | document.body | Portal container element. Content is portaled to document.body by default. |
| <ApiBadge label="trapFocus" />    | <code>boolean</code>                                                                      | true          | Enable focus trapping                                                      |
| <ApiBadge label="restoreFocus" /> | <code>boolean</code>                                                                      | true          | Restore focus on close                                                     |
| <ApiBadge label="initialFocus" /> | <code>HTMLElement &#124; (() =&gt; HTMLElement)</code>                                    | -             | Element to focus on open                                                   |
| <ApiBadge label="finalFocus" />   | <code>HTMLElement &#124; (() =&gt; HTMLElement)</code>                                    | -             | Element to focus on close                                                  |

#### Events {#drawer-panel-events}

| Name                                      | Type                                           | Default | Description                                                |
| ----------------------------------------- | ---------------------------------------------- | ------- | ---------------------------------------------------------- |
| <ApiBadge label="onOpenAutoFocus" />      | <code>(event: Event) =&gt; void</code>         | -       | Callback before auto-focus                                 |
| <ApiBadge label="onCloseAutoFocus" />     | <code>(event: Event) =&gt; void</code>         | -       | Callback before focus restore                              |
| <ApiBadge label="onEscapeKeyDown" />      | <code>(event: KeyboardEvent) =&gt; void</code> | -       | Escape key handler                                         |
| <ApiBadge label="onPointerDownOutside" /> | <code>(event: PointerEvent) =&gt; void</code>  | -       | Outside click handler                                      |
| <ApiBadge label="onInteractOutside" />    | <code>(event: PointerEvent) =&gt; void</code>  | -       | Outside interaction handler with preventDefault capability |

#### Data attributes {#drawer-panel-data-attributes}

| Attribute                                       | Applied when | Purpose                                                 |
| ----------------------------------------------- | ------------ | ------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot.   |
| <ApiBadge label="data-state" />                 | Always       | `"open"` or `"closed"` — drives slide/scale animations. |
| <ApiBadge label="data-placement" />             | Always       | Reflects placement for directional CSS transitions.     |

### Drawer.Header {#drawer-header}

#### Props {#drawer-header-props}

| Name                            | Type                                                                                      | Default | Description         |
| ------------------------------- | ----------------------------------------------------------------------------------------- | ------- | ------------------- |
| <ApiBadge label="headerType" /> | <code><a href="#drawer-td-drawerheadertype">DrawerHeaderType</a></code>                   | 'basic' | Type of the header. |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                                  | -       |                     |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -       |                     |

#### Data attributes {#drawer-header-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |
| <ApiBadge label="data-header-type" />           | Always       | Reflects the resolved `headerType` prop.              |

### Drawer.Title {#drawer-title}

#### Props {#drawer-title-props}

| Name                            | Type                                                                                      | Default | Description                                                                                                                                              |
| ------------------------------- | ----------------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                                  | -       |                                                                                                                                                          |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -       |                                                                                                                                                          |
| <ApiBadge label="level" />      | <code>number</code>                                                                       | 5       | Semantic heading level (1-6). Sets the rendered tag (`h1`-`h6`) and `data-level` for the document outline; the visual size is fixed regardless of level. |

#### Data attributes {#drawer-title-data-attributes}

| Attribute                                       | Applied when | Purpose                                                                                   |
| ----------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot.                                     |
| <ApiBadge label="data-level" />                 | Always       | Reflects the resolved `level` prop for the document outline (does not change typography). |

### Drawer.Description {#drawer-description}

#### Data attributes {#drawer-description-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |

### Drawer.Body {#drawer-body}

#### Data attributes {#drawer-body-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |

### Drawer.Footer {#drawer-footer}

#### Props {#drawer-footer-props}

| Name                            | Type                                                                                      | Default | Description         |
| ------------------------------- | ----------------------------------------------------------------------------------------- | ------- | ------------------- |
| <ApiBadge label="footerType" /> | <code><a href="#drawer-td-drawerfootertype">DrawerFooterType</a></code>                   | 'basic' | Type of the footer. |
| <ApiBadge label="classNames" /> | <code>Partial&lt;Record&lt;"root", string&gt;&gt;</code>                                  | -       |                     |
| <ApiBadge label="slotProps" />  | <code>Partial&lt;Record&lt;"root", React.HTMLAttributes&lt;HTMLElement&gt;&gt;&gt;</code> | -       |                     |

#### Data attributes {#drawer-footer-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |
| <ApiBadge label="data-footer-type" />           | Always       | Reflects the resolved `footerType` prop.              |

### Drawer.Close {#drawer-close}

#### Data attributes {#drawer-close-data-attributes}

| Attribute                                       | Applied when | Purpose                                               |
| ----------------------------------------------- | ------------ | ----------------------------------------------------- |
| <ApiBadge label="data-slot=&quot;root&quot;" /> | Always       | Stable selector for wrapper styling on the root slot. |

### Type Definitions {#drawer-type-definitions}

| Name                                                                                           | Definition                                                                           |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| <span id="drawer-td-drawerplacement"><ApiBadge label="DrawerPlacement" /></span>               | <code>'left' &#124; 'right' &#124; 'top' &#124; 'bottom' &#124; 'full-screen'</code> |
| <span id="drawer-td-draweroverlayintensity"><ApiBadge label="DrawerOverlayIntensity" /></span> | <code>'lightest' &#124; 'light' &#124; 'base' &#124; 'dark' &#124; 'darkest'</code>  |
| <span id="drawer-td-drawerheadertype"><ApiBadge label="DrawerHeaderType" /></span>             | <code>'basic' &#124; 'divided' &#124; 'light' &#124; 'dark' &#124; 'primary'</code>  |
| <span id="drawer-td-drawerfootertype"><ApiBadge label="DrawerFooterType" /></span>             | <code>'basic' &#124; 'divided' &#124; 'light'</code>                                 |

{/* api-tables:end */}
