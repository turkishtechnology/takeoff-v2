---
'@takeoff-ui/react-spar': patch
'@takeoff-design/tokens': patch
---

Create new Dropdown component, including a scrollable `Dropdown.Viewport` region
that keeps the highlighted item in view during keyboard navigation.

Requires the upstream Spar `DropdownMenuViewport` support from
turkishtechnology/spar#188.
