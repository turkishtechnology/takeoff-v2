---
'@takeoff-ui/react-spar': minor
'@takeoff-design/tokens': minor
---

Add the Progress compound component (`Progress`, `Progress.Indicator`) with linear and circular appearances, small/base/large sizes, fill color variants, a disabled state, and automatic label/disabled wiring when composed inside a `Field` — plus the matching `tk-progress` recipe in the tokens package.
