import type { ChipAppearance, ChipSize, ChipVariant } from './types';

export const DEFAULT_VARIANT: ChipVariant = 'primary';
export const DEFAULT_APPEARANCE: ChipAppearance = 'filled';
export const DEFAULT_SIZE: ChipSize = 'base';
export const DEFAULT_REMOVE_LABEL = 'Remove';
