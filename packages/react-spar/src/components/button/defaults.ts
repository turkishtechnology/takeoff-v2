import type { ButtonAppearance, ButtonSize, ButtonVariant } from './types';

export const DEFAULT_VARIANT: ButtonVariant = 'primary';
export const DEFAULT_APPEARANCE: ButtonAppearance = 'filled';
export const DEFAULT_SIZE: ButtonSize = 'base';
