import type { RadioPosition, RadioSize, RadioType } from './types';

export const DEFAULT_SIZE: RadioSize = 'base';
export const DEFAULT_TYPE: RadioType = 'default';
export const DEFAULT_POSITION: RadioPosition = 'left';
