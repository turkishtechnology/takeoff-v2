import type { BadgeAppearance, BadgeSize, BadgeVariant } from './types';

export const DEFAULT_VARIANT: BadgeVariant = 'primary';
export const DEFAULT_APPEARANCE: BadgeAppearance = 'filled';
export const DEFAULT_SIZE: BadgeSize = 'base';
